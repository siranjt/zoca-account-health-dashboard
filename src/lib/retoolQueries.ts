// AUTO-GENERATED from the Retool "Customer Dashboard" export.
// Every query the Retool app runs, parameterized by entity id.
// __ENTITY_ID__ is substituted with the account entity_id at runtime.
// Sections hand-assigned per Retool layout; do not rely on keyword guessing.

export interface RetoolQuery {
  name: string;
  section: string;
  runnable: boolean; // false = depends on other queries (SQL shown, not executed)
  deps: string[];
  sql: string;
}

export const RETOOL_SECTIONS = ["Profile & GBP","GBP Content","Reviews","Funnel & Leads","Rankings & SEO","Payments & Billing","Scheduling & Bookings","Calls & Chatbot","App Engagement","Onboarding & Team","Contact, IDs & Requests","Other"] as const;

export const RETOOL_QUERIES: RetoolQuery[] = [
  {"name":"chrone_uid","section":"Profile & GBP","runnable":true,"deps":[],"sql":"SELECT uid as chrone_uid, cmu.created_at\nFROM chrone_mapping.uid_location cmu\nJOIN gbp.locations gl ON gl.name = cmu.location_name\nWHERE gl.entity_id = '__ENTITY_ID__'"},
  {"name":"GBP_Metrics","section":"Profile & GBP","runnable":true,"deps":[],"sql":"WITH locations as (\n    SELECT entity_id, name \n    FROM gbp.locations \n),\ndata as (\n  SELECT \n      --loc.entity_id, \n      DATE_TRUNC('week', gm.\"metrics_timestamp\"::date)::date as week_start, \n      sum(gm.\"desktop_map_clicks\" + gm.\"desktop_search_clicks\" + gm.\"mobile_map_clicks\" + gm.\"mobile_search_clicks\") as profile_clicks,\n      sum(gm.\"website_clicks\") as website_clicks,\n      sum(gm.\"call_clicks\") as call_clicks,\n      sum(gm.\"business_direction_requests\") as direction_requests,\n      sum(gm.\"website_clicks\"+gm.\"call_clicks\"+gm.\"business_direction_requests\") as total_interactions\n      \n  FROM gbp.metrics gm\n  LEFT JOIN locations loc ON gm.\"location_name\" = loc.name \n  WHERE loc.entity_id = '__ENTITY_ID__'\n  group by 1\n),\nleads as (\n    SELECT  DATE_TRUNC('week', created_at::date)::date as week_start, count(distinct CONCAT(attributes->>'country_code','-',attributes->>'phone_number')) as leads\n    FROM\n  \"website\".\"booking_enquiries\"\nWHERE source = 'WEBSITE' AND is_test_lead = false\nAND entity_id = '__ENTITY_ID__'\n    GROUP BY 1\n),\nweek_series AS (\n    SELECT \n        generate_series(\n            COALESCE((SELECT MIN(week_start) FROM data), (SELECT MIN(week_start) FROM leads)), \n            DATE_TRUNC('week', CURRENT_DATE::date)::date, \n            '1 week'\n        )::date AS week_start\n)\nSELECT  \n    ws.week_start,\n    profile_clicks,\n    website_clicks,\n    call_clicks,\n    direction_requests,\n    total_interactions,\n    coalesce(leads,0) as leads,\n    coalesce(website_clicks*1.0/NULLIF(profile_clicks, 0), 0) as wc_by_pc,\n    coalesce(call_clicks*1.0/NULLIF(profile_clicks, 0), 0) as cc_by_pc,\n    coalesce(direction_requests*1.0/NULLIF(profile_clicks, 0), 0) as dc_by_pc,\n    --leads by wc and leads by pc\n    coalesce(leads*1.0/NULLIF(website_clicks, 0), 0) as leads_by_wc,\n    coalesce(leads*1.0/NULLIF(profile_clicks, 0), 0) as leads_by_pc\nFROM week_series ws\nLEFT JOIN data ON ws.week_start = data.week_start\nLEFT JOIN leads ON ws.week_start = leads.week_start\nWHERE ws.week_start > CURRENT_DATE - INTERVAL '2 years'\norder by 1 desc"},
  {"name":"GBP_metrics_monthly","section":"Profile & GBP","runnable":true,"deps":[],"sql":"WITH locations as (\n    SELECT entity_id, name \n    FROM gbp.locations \n),\ndata as (\n  SELECT \n      --loc.entity_id, \n      DATE_TRUNC('month', gm.\"metrics_timestamp\"::date)::date as month_start, \n      sum(gm.\"desktop_map_clicks\" + gm.\"desktop_search_clicks\" + gm.\"mobile_map_clicks\" + gm.\"mobile_search_clicks\") as profile_clicks,\n      sum(gm.\"website_clicks\") as website_clicks,\n      sum(gm.\"call_clicks\") as call_clicks,\n      sum(gm.\"business_direction_requests\") as direction_requests,\n  (sum(gm.\"business_direction_requests\")+sum(gm.\"call_clicks\")+sum(gm.\"website_clicks\")) as total_interactions\n      \n  FROM gbp.metrics gm\n  LEFT JOIN locations loc ON gm.\"location_name\" = loc.name \n  WHERE loc.entity_id = '__ENTITY_ID__'\n  group by 1\n),\nleads as (\n    SELECT  DATE_TRUNC('month', created_at::date)::date as month_start, count(distinct CONCAT(attributes->>'country_code','-',attributes->>'phone_number')) as leads\n    FROM\n  \"website\".\"booking_enquiries\"\nWHERE source = 'WEBSITE' AND is_test_lead = false\nAND entity_id = '__ENTITY_ID__'\n    GROUP BY 1\n),\nmonth_series AS (\n    SELECT \n        generate_series(\n            COALESCE((SELECT MIN(month_start) FROM data), (SELECT MIN(month_start) FROM leads)), \n            DATE_TRUNC('month', CURRENT_DATE::date)::date, \n            '1 month'\n        )::date AS month_start\n)\nSELECT  \n    ws.month_start,\n    profile_clicks,\n    website_clicks,\n    call_clicks,\n    direction_requests,\n    total_interactions,\n    coalesce(leads,0) as leads,\n    coalesce(website_clicks*1.0/NULLIF(profile_clicks, 0), 0) as wc_by_pc,\n    coalesce(call_clicks*1.0/NULLIF(profile_clicks, 0), 0) as cc_by_pc,\n    coalesce(direction_requests*1.0/NULLIF(profile_clicks, 0), 0) as dc_by_pc,\n    --leads by wc and leads by pc\n    coalesce(leads*1.0/NULLIF(website_clicks, 0), 0) as leads_by_wc,\n    coalesce(leads*1.0/NULLIF(profile_clicks, 0), 0) as leads_by_pc\nFROM month_series ws\nLEFT JOIN data ON ws.month_start = data.month_start\nLEFT JOIN leads ON ws.month_start = leads.month_start\nWHERE ws.month_start > CURRENT_DATE - INTERVAL '2 years'\norder by 1 desc"},
  {"name":"keyword_impressions","section":"Profile & GBP","runnable":true,"deps":[],"sql":"WITH locations AS (\n    SELECT entity_id, name \n    FROM gbp.locations\n)\nSELECT \n    TO_CHAR(TO_DATE(ki.year::text || '-' || ki.month::text, 'YYYY-MM'), 'Month YYYY') AS month,\n    ki.keyword,\n    ki.value AS impressions\nFROM \n    gbp.keyword_impressions ki\nJOIN \n    locations loc ON loc.name::text = ki.location_name::text\nWHERE \n    loc.entity_id = '__ENTITY_ID__'\nORDER BY \n    ki.year ASC, ki.month ASC;"},
  {"name":"Location_data","section":"Profile & GBP","runnable":true,"deps":[],"sql":"WITH location_entity AS (\n    SELECT \n        entity_id,\n        name,\n        address,\n        category\n    FROM entities.locations \n),\ngbp_urls AS (\n    SELECT DISTINCT ON (entity_id, name)\n        entity_id,\n        name,\n        uri_values[0]->>'uri' AS url,\n        created_at\n    FROM gbp.attributes\n    WHERE name IN ('attributes/url_appointment', 'attributes/url_menu')\n    ORDER BY entity_id, name, created_at DESC\n),\nappointment_url AS (\n    SELECT entity_id, url AS appointment_url\n    FROM gbp_urls\n    WHERE name = 'attributes/url_appointment'\n),\nmenu_url AS (\n    SELECT entity_id, url AS menu_url\n    FROM gbp_urls\n    WHERE name = 'attributes/url_menu'\n)\nSELECT \n  gl.title as GBP_title,\n  gl.name as location_name,\n  gl.metadata::json #>> array [ 'maps_uri' ] as map_link,\n  gl.metadata::json #>> array [ 'place_id' ] as place_id,\n  gl.metadata::json #>> array ['duplicate_location'] as duplicate_location,\n  gl.metadata::json #>> array [ 'has_voice_of_merchant' ] as verification_status,\n  gl.metadata::json #>> array [ 'new_review_uri' ] as review_link,\n  gl.storefront_address::json #>> array [ 'locality' ] as City,\n  gl.storefront_address::json #>> array [ 'administrative_area' ] as State,\n  substring((gl.categories::json #>> array['primary_category'])::json->>'name' FROM ':(.*)$') as primary_category_name,\n  (gl.categories::json #>> array['primary_category'])::json->>'display_name' as primary_category_display_name,\n  split_part(gl.website_uri, '?', 1) as website_uri,\n  au.appointment_url,\n  mu.menu_url,\n  gl.profile::json #>> array ['description' ] as GBP_description,\n  gl.latlng::json #>> array['latitude'] as latitude,\n  gl.latlng::json #>> array['longitude'] as longitude,\n  gl.custom_completion_score,\n  CONCAT(\n    (gl.open_info::json #>> array ['opening_date'])::json->>'day', '-',\n    (gl.open_info::json #>> array ['opening_date'])::json->>'month', '-',\n    (gl.open_info::json #>> array ['opening_date'])::json->>'year'\n  ) as opening_date,\n  el.created_at::date\nFROM gbp.locations gl\nLEFT JOIN entities.locations el ON el.entity_id = gl.entity_id\nLEFT JOIN appointment_url au ON au.entity_id = gl.entity_id\nLEFT JOIN menu_url mu ON mu.entity_id = gl.entity_id\nWHERE gl.entity_id = '__ENTITY_ID__'"},
  {"name":"location_names","section":"Profile & GBP","runnable":true,"deps":[],"sql":"SELECT \n  el.entity_id,\n  coalesce( (gl.metadata ->> 'has_voice_of_merchant')::boolean , false ) verification_status,\n  el.name\nFROM entities.locations el \nLEFT JOIN gbp.locations gl  \nON el.entity_id = gl.entity_id\nWHERE el.entity_id NOT IN ('b9dacf77-cfcb-4bd2-805b-061ac4ffbc0b')\nGROUP BY 1,2,3\nORDER BY 3"},
  {"name":"query29","section":"Profile & GBP","runnable":true,"deps":[],"sql":"WITH keyword_data AS (\n  SELECT\n    ki.month,\n    ki.year,\n    CASE\n      WHEN ki.is_relevant = false THEN 'Irrelevant'\n      ELSE COALESCE(ki.theme, 'Uncategorized')\n    END AS display_theme,\n    ki.value\n  FROM gbp.keyword_impressions ki\n  JOIN gbp.locations gl ON gl.name = ki.location_name\n  WHERE gl.entity_id = '__ENTITY_ID__'::uuid\n  and ki.value <> 15\n),\nmonthly_totals AS (\n  SELECT year, month, SUM(value) AS total_impressions\n  FROM keyword_data\n  GROUP BY year, month\n),\ntheme_totals AS (\n  SELECT year, month, display_theme, SUM(value) AS theme_impressions\n  FROM keyword_data\n  GROUP BY year, month, display_theme\n)\nSELECT\n  tt.year || '-' || LPAD(tt.month::text, 2, '0') AS month_label,\n  tt.display_theme AS theme,\n  tt.theme_impressions,\n  ROUND((tt.theme_impressions * 100.0 / NULLIF(mt.total_impressions, 0)), 2) AS pct_impressions\nFROM theme_totals tt\nJOIN monthly_totals mt ON mt.year = tt.year AND mt.month = tt.month\nORDER BY tt.year, tt.month, pct_impressions DESC"},
  {"name":"query30","section":"Profile & GBP","runnable":true,"deps":[],"sql":"WITH base AS (\n    SELECT\n        el.entity_id,\n        (el.open_info #>> '{opening_date,month}') IS NOT NULL   AS is_opening_date_added,\n        sc.entity_id IS NOT NULL                                 AS is_storefront_image_added,\n        wg.entity_id IS NOT NULL                                 AS is_website_alive,\n        gv.entity_id IS NOT NULL                                 AS is_gbp_verified,\n        ri.entity_id IS NOT NULL                                 AS has_review_invite_or_ra,\n        ap.entity_id IS NOT NULL                                 AS has_uploaded_photo_on_app,\n        ig.entity_id IS NOT NULL                                 AS has_added_website_to_instagram,\n        qr.entity_id IS NOT NULL                                 AS has_downloaded_review_qr_poster\n    FROM entities.locations el\n    LEFT JOIN (\n        SELECT DISTINCT entity_id FROM gbp.media_items\n        WHERE category = 'EXTERIOR' AND (is_deleted IS NULL OR is_deleted = false)\n    ) sc ON sc.entity_id = el.entity_id\n    LEFT JOIN (\n        SELECT DISTINCT gl.entity_id\n        FROM gbp.locations gl\n        JOIN entities.product_entities epe ON epe.entity_id = gl.entity_id\n        JOIN website.status ws ON ws.website_id::TEXT = epe.id::TEXT AND epe.product_id = 1\n        JOIN entities.entities ee ON ee.entity_id = gl.entity_id AND NOT ee.is_test AND ee.is_active\n        JOIN website.websites ww ON ww.website_id = ws.website_id\n        LEFT JOIN website.custom_domains cd ON cd.website_id = ws.website_id AND cd.is_active = true\n        WHERE (gl.metadata ->> 'has_voice_of_merchant')::boolean = true\n          AND (\n              (cd.custom_domain IS NOT NULL AND cd.is_active = true\n                  AND gl.website_uri LIKE '%' || cd.custom_domain || '%')\n              OR gl.website_uri LIKE '%' || ws.website_url || '%'\n          )\n    ) wg ON wg.entity_id = el.entity_id\n    LEFT JOIN (\n        SELECT DISTINCT entity_id FROM gbp.locations\n        WHERE (metadata ->> 'has_voice_of_merchant')::boolean = true\n    ) gv ON gv.entity_id = el.entity_id\n    LEFT JOIN (\n        SELECT DISTINCT entity_id FROM (\n            SELECT entity_id FROM reviews.review_invites WHERE is_deleted = false\n            UNION\n            SELECT entity_id FROM clients.client_files\n            UNION\n            SELECT el2.entity_id\n            FROM auth.auth aa\n            JOIN entities.entity_relationships eer ON eer.entity_1_id = aa.entity_id\n            JOIN entities.locations el2 ON el2.entity_id = eer.entity_2_id\n            WHERE aa.platform = 'SQUARE' AND eer.relationship_type = 'HAS_LOCATION'\n        ) combined\n    ) ri ON ri.entity_id = el.entity_id\n    LEFT JOIN (\n        SELECT DISTINCT me.entity_id\n        FROM media.media_entities me\n        JOIN media.media mm ON mm.id = me.media_id AND mm.is_active = true\n        JOIN media.sources ms ON ms.id = mm.source_id AND ms.type = 'APP'\n    ) ap ON ap.entity_id = el.entity_id\n    LEFT JOIN (\n        SELECT DISTINCT entity_id FROM entities.preferences\n        WHERE attribute = 'discoverySocial.instagram.markAsDone' AND value = 'true' AND is_deleted = false\n    ) ig ON ig.entity_id = el.entity_id\n    LEFT JOIN (\n        SELECT DISTINCT entity_id FROM entities.preferences\n        WHERE attribute = 'ep.activationSteps.status.reviewQrPoster' AND value = 'COMPLETED' AND is_deleted = false\n    ) qr ON qr.entity_id = el.entity_id\n    WHERE el.entity_id = '__ENTITY_ID__'  -- paste your entity ID here\n)\n\nSELECT step_name AS \"Activation Step\", step_completed AS \"Completed\"\nFROM base\nCROSS JOIN LATERAL (\n    VALUES\n        ('1. Opening Date Added',           is_opening_date_added),\n        ('2. Storefront Image on GBP',      is_storefront_image_added),\n        ('3. Website Linked in GBP',        is_website_alive),\n        ('4. GBP Verified',                 is_gbp_verified),\n        ('5. Review Invite / RA Enabled',   has_review_invite_or_ra),\n        ('6. Photo Uploaded via App',       has_uploaded_photo_on_app),\n        ('7. Instagram Website Added',      has_added_website_to_instagram),\n        ('8. QR Poster Downloaded',         has_downloaded_review_qr_poster)\n) AS steps(step_name, step_completed)\nORDER BY step_name;"},
  {"name":"query31","section":"Profile & GBP","runnable":true,"deps":[],"sql":"SELECT * FROM cx.health_score WHERE entity_id = '__ENTITY_ID__'"},
  {"name":"coverphoto","section":"GBP Content","runnable":true,"deps":[],"sql":"SELECT google_url \nFROM gbp.media_items\nWHERE entity_id = '__ENTITY_ID__'\nAND is_deleted = false \nAND category = 'COVER'"},
  {"name":"photos","section":"GBP Content","runnable":true,"deps":[],"sql":"WITH base AS (\n    SELECT \n        entity_id,\n        create_time AS created_at,\n        deleted_at AS updated_at,\n        is_deleted\n    FROM gbp.media_items\n    WHERE entity_id = '__ENTITY_ID__'\n),\nplus AS (\n    SELECT \n        entity_id,\n        created_at::date AS date,\n        1 AS plus\n    FROM base\n),\nminus AS (\n    SELECT \n        entity_id,\n        updated_at::date AS date,\n        -1 AS plus\n    FROM base\n    WHERE is_deleted = TRUE\n),\nsummer AS (\n    SELECT entity_id, date, plus\n    FROM plus\n    UNION ALL\n    SELECT entity_id, date, plus\n    FROM minus\n),\nsummed AS (\n    SELECT\n        entity_id,\n        DATE_TRUNC('week', date)::date AS week_start,\n        SUM(plus) AS photos_sum\n    FROM summer\n    GROUP BY entity_id, DATE_TRUNC('week', date)::date\n),\n-- Generate all weekly start dates for the last 2 years\nweeks AS (\n    SELECT \n        GENERATE_SERIES(\n            DATE_TRUNC('week', CURRENT_DATE - INTERVAL '2 years'),\n            DATE_TRUNC('week', CURRENT_DATE),\n            INTERVAL '1 week'\n        )::date AS week_start\n),\n-- Join all weeks with your summed data (left join so missing weeks get 0)\njoined AS (\n    SELECT\n        w.week_start,\n        COALESCE(s.photos_sum, 0) AS photos_sum,\n        SUM(COALESCE(s.photos_sum, 0)) OVER (ORDER BY w.week_start) AS photos_cum_sum\n    FROM weeks w\n    LEFT JOIN summed s \n           ON s.week_start = w.week_start\n    WHERE w.week_start > CURRENT_DATE - INTERVAL '2 years'\n)\n-- Finally, remove any rows that occur before the first non-zero cumulative total\nSELECT\n    week_start,\n    photos_sum,\n    photos_cum_sum\nFROM joined\nWHERE photos_cum_sum > 0\nORDER BY week_start DESC;"},
  {"name":"posts_count","section":"GBP Content","runnable":true,"deps":[],"sql":"WITH posts AS (\n  SELECT date_trunc('week', gp.create_time::date) as week_start,\n  count(*) as posts\n  FROM  gbp.posts gp\nJOIN gbp.locations gl ON gl.name = gp.location_name\nWHERE gl.entity_id = '__ENTITY_ID__' AND is_deleted = false and state = 'LIVE'\nGROUP BY 1\n),\n\nweek_series AS (\n    SELECT \n        generate_series(\n            (SELECT MIN(week_start) FROM posts), \n            DATE_TRUNC('week', CURRENT_DATE::date)::date, \n            '1 week'\n        )::date AS week_start\n)\nSELECT \n    ws.week_start,\n    COALESCE(rd.posts, 0) AS posts,\n    SUM(COALESCE(rd.posts, 0)) OVER (ORDER BY ws.week_start) AS cumsum\nFROM \n    week_series ws\nLEFT JOIN \n    posts rd ON ws.week_start = rd.week_start\nORDER BY \n    ws.week_start DESC;"},
  {"name":"posts_raw","section":"GBP Content","runnable":true,"deps":[],"sql":"SELECT summary, event, offer, media, call_to_action, topic_type, state, gp.create_time::date\nFROM gbp.posts gp\nJOIN gbp.locations gl ON gl.name = gp.location_name\nWHERE gl.entity_id = '__ENTITY_ID__' AND is_deleted = false\nORDER BY gp.create_time::date DESC"},
  {"name":"profilephoto","section":"GBP Content","runnable":true,"deps":[],"sql":"SELECT google_url \nFROM gbp.media_items\nWHERE entity_id = '__ENTITY_ID__'\nAND is_deleted = false \nAND category = 'PROFILE'"},
  {"name":"query32","section":"GBP Content","runnable":true,"deps":[],"sql":"SELECT \n    COALESCE(u.total_uploaded, 0)                                      AS photos_uploaded_by_sp,\n    COALESCE(ss.photos_on_gbp, 0)                                     AS photos_uploaded_on_gbp,\n    COALESCE(u.total_uploaded, 0) - COALESCE(ss.photos_on_gbp, 0)     AS photos_left_in_bank\nFROM entities.entities ee\nLEFT JOIN (\n    SELECT mme.entity_id, COUNT(*) AS total_uploaded\n    FROM media.media_entities mme\n    JOIN media.media mm ON mme.media_id = mm.id\n    WHERE mm.is_active = true\n    GROUP BY mme.entity_id\n) u ON u.entity_id = ee.entity_id\nLEFT JOIN (\n    SELECT entity_id,\n        COUNT(*) FILTER (WHERE status = 'POSTED') AS photos_on_gbp\n    FROM gbp.scheduled_media\n    GROUP BY entity_id\n) ss ON ss.entity_id = ee.entity_id\nWHERE ee.entity_id = '__ENTITY_ID__'::uuid"},
  {"name":"query24","section":"Reviews","runnable":true,"deps":[],"sql":"SELECT \n  preferences->>'isAutomatedReviewReply' as AutomatedReviewReplyPreference\nFROM reviews.review_preferences \nwhere entity_id = '__ENTITY_ID__'"},
  {"name":"review_automation","section":"Reviews","runnable":true,"deps":[],"sql":"WITH selected_entity AS (\n  -- Start with the selected entity to ensure we always get a result\n  SELECT entity_id::uuid AS entity_id\n  FROM entities.entities\n  WHERE entity_id::text = '__ENTITY_ID__'\n    AND is_test = 'false'\n    AND is_active = 'true'\n),\nreview_automation_data AS (\n  SELECT\n    se.entity_id,\n    COUNT(DISTINCT c.id) AS total_clients,\n    COUNT(\n      DISTINCT CASE\n        WHEN ec.created_at >= CURRENT_DATE - interval '30 days' \n         AND ec.created_at < CURRENT_DATE - interval '4 days' THEN c.id\n      END\n    ) AS review_automation_sent_count,\n    COUNT(\n      DISTINCT CASE\n        WHEN ec.created_at IS NOT NULL THEN c.id\n      END\n    ) AS total_review_automation_exhausted_clients\n  FROM\n    selected_entity se\n    LEFT JOIN clients.clients c ON c.entity_id = se.entity_id\n      AND c.source IN ('SQUARE', 'FILE')\n      AND (c.latest_activity ->> 'createdAt') IS NOT NULL\n      AND c.is_test = 'false'\n    LEFT JOIN active_campaign.exported_clients ec ON c.entity_id = ec.entity_id\n      AND c.id = ec.client_id\n  GROUP BY\n    se.entity_id\n)\nSELECT \n  CASE\n    WHEN (\n      COALESCE(rp.preferences ->> 'isAutomatedReviewReply', 'false') = 'true'\n      OR COALESCE(ra.total_clients, 0) > 0\n    ) THEN True\n    ELSE False\n  END AS ra_active,\n  COALESCE(ra.total_clients, 0) AS total_clients,\n  COALESCE(ra.review_automation_sent_count, 0) AS review_automation_sent_last_30_days,\n  COALESCE(ra.total_review_automation_exhausted_clients, 0) AS total_review_automation_exhausted_clients\nFROM selected_entity se\nLEFT JOIN review_automation_data ra ON se.entity_id = ra.entity_id\nLEFT JOIN reviews.review_preferences rp ON se.entity_id = rp.entity_id"},
  {"name":"Review_numbers","section":"Reviews","runnable":true,"deps":[],"sql":"WITH review_data as (\n  SELECT \n  DATE_TRUNC('week', review_time::date)::date as week_start,\n  count(*) as reviews\n  FROM\n    \"reviews\".\"reviews\" \n  where entity_id = '__ENTITY_ID__' and platform = 'gbp'\nand is_deleted = false\n  group by 1\n),\nweek_series AS (\n    SELECT \n        generate_series(\n            (SELECT MIN(week_start) FROM review_data), \n            DATE_TRUNC('week', CURRENT_DATE::date)::date, \n            '1 week'\n        )::date AS week_start\n),\nfinale AS (\n  SELECT \n    ws.week_start,\n    COALESCE(rd.reviews, 0) AS reviews,\n    SUM(COALESCE(rd.reviews, 0)) OVER (ORDER BY ws.week_start) AS cumsum\nFROM \n    week_series ws\nLEFT JOIN \n    review_data rd ON ws.week_start = rd.week_start\nORDER BY \n    ws.week_start DESC)\n\nSELECT * FROM finale WHERE week_start > CURRENT_DATE - interval '3 months'"},
  {"name":"review_numbers_per_month","section":"Reviews","runnable":true,"deps":[],"sql":"WITH review_data as (\n  SELECT \n  DATE_TRUNC('month', review_time::date)::date as week_start,\n  count(*) as reviews\n  FROM\n    \"reviews\".\"reviews\" \n  where entity_id = '__ENTITY_ID__' and platform = 'gbp'\nand is_deleted = false\n  group by 1\n),\nweek_series AS (\n    SELECT \n        generate_series(\n            (SELECT MIN(week_start) FROM review_data), \n            DATE_TRUNC('month', CURRENT_DATE::date)::date, \n            '1 month'\n        )::date AS week_start\n)\nSELECT \n    ws.week_start,\n    COALESCE(rd.reviews, 0) AS reviews,\n    SUM(COALESCE(rd.reviews, 0)) OVER (ORDER BY ws.week_start) AS cumsum\nFROM \n    week_series ws\nLEFT JOIN \n    review_data rd ON ws.week_start = rd.week_start\nORDER BY \n    ws.week_start DESC;"},
  {"name":"Reviewdata","section":"Reviews","runnable":true,"deps":[],"sql":"SELECT \n  review_text,\n  reviewer_name,\n  review_time,\n  rating\n  FROM\n    \"reviews\".\"reviews\" \n  where entity_id = '__ENTITY_ID__' and platform = 'gbp'\nand is_deleted = false"},
  {"name":"completeFunnel","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"WITH gbp_location AS (\n    SELECT \n        entity_id, \n        title as business_name, \n        name as location_code, \n        metadata->>'place_id' as place_id\n    FROM gbp.locations \n    WHERE entity_id = '__ENTITY_ID__'\n),\ngbp_metrics AS (\n    SELECT \n        location_name as location_code,\n        date_trunc('week', metrics_timestamp::date) as week_start,\n        sum(website_clicks) as website_clicks,\n        sum(call_clicks) as call_clicks,\n        sum(business_direction_requests) as direction_clicks,\n        sum(desktop_map_clicks + mobile_map_clicks + desktop_search_clicks + mobile_search_clicks) as total_profile_clicks,\n        sum(website_clicks + call_clicks + business_direction_requests) as total_interactions\n    FROM gbp.metrics gm\n    JOIN gbp_location gl ON gl.location_code = gm.location_name\n    GROUP BY 1, 2\n),\nleads AS (\n    SELECT     \n        wbe.entity_id,\n        date_trunc('week', wbe.created_at::date) as week_start,\n        count(distinct wbe.client_id) AS count_of_leads,\n        count(distinct CASE WHEN wbe.status = 'BOOKED' THEN wbe.client_id END) as count_of_booked_leads,\n        count(distinct CASE WHEN cl.type = 'SMS' THEN wbe.client_id END) as client_contacted_by_sms_by_sp,\n        count(distinct CASE WHEN cl.type = 'CALL' THEN wbe.client_id END) AS client_contacted_by_call_by_sp,\n        count(distinct CASE WHEN cl.type IN ('CALL', 'SMS') THEN wbe.client_id END) AS client_contacted_in_anyway_by_sp\n    FROM website.booking_enquiries wbe\n    JOIN gbp_location gl ON wbe.entity_id = gl.entity_id\n    LEFT JOIN clients.communication_logs cl ON cl.enquiry_id = wbe.id\n    WHERE is_test_lead = false \n    GROUP BY 1, 2\n),\nwebsite_data AS (\n    SELECT \n        entity_id as location_entity_id, \n        DATE_TRUNC('week', week_start_date::date) as week_start,\n        sum(total_page_views) as total_page_views, \n        sum(unique_page_views) as unique_page_views, \n        sum(total_book_now_clicks) as total_book_now_clicks, \n        sum(unique_book_now_clicks) as unique_book_now_clicks\n    FROM (SELECT * FROM mixpanel_website.daily_page_views WHERE entity_id <> 'ffbfcb6a-e981-4082-b7a4-39f1a71b3f')\n    WHERE entity_id::uuid = '__ENTITY_ID__'\n    GROUP BY 1,2\n),\n\n\nweek_series AS (\n    SELECT \n        generate_series(\n            COALESCE(\n                (SELECT MIN(week_start) FROM gbp_metrics), \n                (SELECT MIN(week_start) FROM leads), \n                (SELECT MIN(week_start) FROM website_data)\n            ), \n            DATE_TRUNC('week', CURRENT_DATE::date)::date, \n            interval '1 week'\n        )::date AS week_start\n)\n\nSELECT \n    ws.week_start,\n    gm.total_profile_clicks,\n    gm.total_interactions,\n    gm.website_clicks,\n    wd.unique_page_views,\n    wd.unique_book_now_clicks,\n    ld.client_contacted_in_anyway_by_sp,\n    ld.count_of_leads,\n    ld.count_of_booked_leads\nFROM week_series ws  \nLEFT JOIN gbp_metrics gm ON gm.week_start = ws.week_start  \nLEFT JOIN leads ld ON ld.week_start = ws.week_start   \nLEFT JOIN website_data wd ON wd.week_start = ws.week_start\nORDER BY 1 DESC"},
  {"name":"lead_predictor","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"SELECT \n  created_at,\n  predicted_6_month_leads as \"6_month_lead_prediction\",\n  review_target as weekly_review_target,\n  predicted_6_month_revenue,\n  round((monthly_predictions[2] ->> 'withZocaLeads')::numeric,2) as \"M0_lead_prediction\",\n round((monthly_predictions[3] ->> 'withZocaLeads')::numeric,2) as \"M1_lead_prediction\",\n  round((monthly_predictions[4] ->> 'withZocaLeads')::numeric,2) as \"M2_lead_prediction\",\n round((monthly_predictions[5] ->> 'withZocaLeads')::numeric,2) as \"M3_lead_prediction\",\n  round((monthly_predictions[6] ->> 'withZocaLeads')::numeric,2) as \"M4_lead_prediction\",\n round((monthly_predictions[7] ->> 'withZocaLeads')::numeric,2) as \"M5_lead_prediction\"\nFROM entities.location_insights\nWHERE entity_id = '__ENTITY_ID__' AND predicted_6_month_leads IS NOT NULL AND (metadata ->> 'non_icp' IS NULL OR metadata ->> 'non_icp' != 'true')\n  and id not in (20580, 45889)\nORDER BY created_at ASC \nLIMIT 1"},
  {"name":"leads_table","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"WITH base AS (\n    SELECT\n  be.created_at,\n  CONCAT(be.attributes->>'country_code','-',be.attributes->>'phone_number') AS phone_number,\n  COALESCE(MAX(be.attributes->>'first_name'),MAX(be.attributes->>'name'))     AS first_name,\n  MAX(be.attributes->>'last_name')     AS last_name,\n  MAX(be.attributes->>'customer_type') AS customer_type,\n  be.id as enquiry_id,\n  utm_source,\n  CASE\n    WHEN lower(utm_source) ILIKE '%googlemaps%'\n      OR lower(utm_source) ILIKE '%google-gmb%'\n      OR lower(utm_source) IN ('googleposts','googlesite','gbp','holidaygoogleoffers','omg-gbp') OR utm_source IS NULL OR utm_source = ''\n                                                          THEN 'Google Maps / GBP'\n    WHEN lower(utm_source) IN ('ig','instagram')          THEN 'Instagram'\n    WHEN lower(utm_source) ILIKE '%googleads%'\n      OR lower(utm_source) ILIKE '%google_ad%'\n      OR lower(utm_source) IN ('ad_leads_conversion','google')\n                                                          THEN 'Google Ads'\n    WHEN lower(utm_source) ILIKE '%sms%'                   THEN 'SMS Campaigns'\n    WHEN lower(utm_source) ILIKE '%chatgpt%'\n      OR lower(utm_source) IN ('perplexity','copilot.com','claude','gemini','bard')\n                                                          THEN 'AI Search'\n    WHEN lower(utm_source) ILIKE '%email%'                 THEN 'Email Campaigns'\n    WHEN lower(utm_source) = 'applemaps'                  THEN 'Apple Maps'\n    WHEN lower(utm_source) IN (\n        'fb','fbads','facebook','facebooks',\n        'facebook_lead_ads','facebook_ads_leads'\n      )\n      OR lower(utm_source) ILIKE '%fb_ads%'                THEN 'Facebook / Meta Ads'\n    WHEN lower(utm_source) IN (\n        'attentive','activecampaign','sqmktg_email',\n        'newsletter','ctct','klarna','trustindex'\n      )\n      OR lower(utm_source) ILIKE '%activecampaign%'        THEN 'Other Marketing Tools'\n    WHEN lower(utm_source) = 'yelp'                       THEN 'Yelp'\n    WHEN lower(utm_source) = 'snapchat'                   THEN 'Snapchat'\n    WHEN lower(utm_source) ILIKE '%bing%'                  THEN 'Bing'\n    WHEN lower(utm_source) = 'direct'                     THEN 'Direct'\n    ELSE 'Unknown / Other'\n  END          AS utm_bucket,\n  be.utm_campaign,\n  be.utm_medium,\n  be.referrer,\n  be.status,\n  be.service,\n  sd.latest_reason,\n  be.price\nFROM website.booking_enquiries AS be\nLEFT JOIN LATERAL (\n  SELECT\n    sd_elem->>'reason' AS latest_reason\n  FROM jsonb_array_elements(COALESCE(be.status_details::jsonb, '[]'::jsonb)) AS sd(sd_elem)\n  ORDER BY (sd_elem->>'createdAt')::timestamptz DESC NULLS LAST\n  LIMIT 1\n) AS sd ON TRUE\nWHERE be.source = 'WEBSITE'\n  AND be.is_test_lead = FALSE\n  AND be.entity_id = '__ENTITY_ID__'\nGROUP BY\n  be.created_at,\n  be.id,\n  phone_number,\n  be.utm_source,\n  be.utm_campaign,\n  be.utm_medium,\n  be.referrer,\n  be.status,\n  be.service,\n  sd.latest_reason,\n  be.price\n),\n\nlead_open AS (\n    SELECT \n        be.*,\n        mo.created_at AS lead_opened_at,\n        CASE WHEN mo.created_at IS NOT NULL THEN TRUE ELSE FALSE END AS lead_opened\n    FROM base AS be\n    LEFT JOIN (SELECT lead_id, min(time) as created_at FROM mixpanelzocaappdata.export WHERE event = 'Leads-View-Chat' AND lead_id IS NOT NULL GROUP BY 1) AS mo   \n        ON mo.lead_id::TEXT = be.enquiry_id::TEXT\n),\n\nlead_contacted AS (\n    SELECT \n        lo.*,\n        mc.created_at AS lead_contacted_at,\n        CASE WHEN mc.created_at IS NOT NULL THEN TRUE ELSE FALSE END AS lead_contacted\n    FROM lead_open AS lo\n    LEFT JOIN (SELECT enquiry_id, min(created_at) as created_at FROM clients.communication_logs WHERE type IN ('SMS', 'CALL') GROUP BY 1) AS mc   \n        ON mc.enquiry_id::TEXT = lo.enquiry_id::TEXT\n)\n\nSELECT * FROM lead_contacted;"},
  {"name":"query21","section":"Funnel & Leads","runnable":false,"deps":["lead_predictor"],"sql":"SELECT 'MO' as month, {{ lead_predictor.data.M0_lead_prediction[0] }} as value , {{ lead_predictor.data.M0_lead_prediction[0] }}::numeric as cumsum\n\nUNION ALL \n\nSELECT 'M1' as month, {{ lead_predictor.data.M1_lead_prediction[0] }} as value , {{ lead_predictor.data.M0_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M1_lead_prediction[0] }}::numeric as cumsum\n\nUNION ALL \n\nSELECT 'M2' as month, {{ lead_predictor.data.M2_lead_prediction[0] }} as value , {{ lead_predictor.data.M0_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M1_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M2_lead_prediction[0]}}::numeric as cumsum\n\nUNION ALL \n\nSELECT 'M3' as month, {{ lead_predictor.data.M3_lead_prediction[0] }} as value , {{ lead_predictor.data.M0_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M1_lead_prediction[0]}}::numeric + {{   lead_predictor.data.M2_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M3_lead_prediction[0]}}::numeric as cumsum\n\nUNION ALL \n\nSELECT 'M4' as month, {{ lead_predictor.data.M4_lead_prediction[0] }} as value , {{ lead_predictor.data.M0_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M1_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M2_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M3_lead_prediction[0]}}::numeric + {{  lead_predictor.data.M4_lead_prediction[0]}}::numeric as cumsum\n\nUNION ALL \n\nSELECT 'M5' as month, {{ lead_predictor.data.M5_lead_prediction[0] }} as value , {{ lead_predictor.data.M0_lead_prediction[0] }}::numeric + {{  lead_predictor.data.M1_lead_prediction[0] }}::numeric+ {{  lead_predictor.data.M2_lead_prediction[0] }}::numeric+ {{  lead_predictor.data.M3_lead_prediction[0] }}::numeric + {{  lead_predictor.data.M4_lead_prediction[0] }}::numeric+ {{  lead_predictor.data.M5_lead_prediction[0]}}::numeric as cumsum"},
  {"name":"query22","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"SELECT \n *\nFROM entities.location_insights\nWHERE entity_id::text = '__ENTITY_ID__'::text"},
  {"name":"query28","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"WITH leads AS (\n  SELECT COUNT(*) AS total_leads\n  FROM website.booking_enquiries be\n  WHERE be.entity_id = '__ENTITY_ID__'\n    AND be.is_test_lead = false\n    AND be.created_at >= NOW() - INTERVAL '30 days'\n),\nbookings_summary AS (\n  SELECT \n    COUNT(DISTINCT b.id) FILTER (WHERE b.status IN ('BOOKED', 'CONFIRMED', 'COMPLETED')) AS confirmed_bookings,\n    COUNT(DISTINCT b.id) FILTER (WHERE b.status IN ('CANCELLED', 'NO_SHOW')) AS cancellations\n  FROM scheduling.bookings b  WHERE b.entity_id = '__ENTITY_ID__'\n    AND b.created_at >= NOW() - INTERVAL '30 days'\n),\nrevenue AS (\n  SELECT COALESCE(SUM(bi.amount), 0) AS total_revenue\n  FROM scheduling.booking_items bi\n  JOIN scheduling.bookings b ON bi.booking_id = b.id\n  WHERE b.entity_id = '__ENTITY_ID__'\n    AND b.status IN ('BOOKED', 'CONFIRMED', 'COMPLETED')\n    AND bi.start_at >= NOW() - INTERVAL '30 days'\n)\nSELECT \n  l.total_leads,\n  bs.confirmed_bookings,\n  r.total_revenue,\n  bs.cancellations\nFROM leads l, bookings_summary bs, revenue r;"},
  {"name":"query33","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"SELECT\n  CASE\n    WHEN lower(utm_source) ILIKE '%googlemaps%'\n      OR lower(utm_source) ILIKE '%google-gmb%'\n      OR lower(utm_source) IN ('googleposts','googlesite','gbp','holidaygoogleoffers','omg-gbp') OR utm_source IS NULL OR utm_source = ''\n                                                          THEN 'Google Maps / GBP'\n    WHEN lower(utm_source) IN ('ig','instagram')          THEN 'Instagram'\n    WHEN lower(utm_source) ILIKE '%googleads%'\n      OR lower(utm_source) ILIKE '%google_ad%'\n      OR lower(utm_source) IN ('ad_leads_conversion','google')\n                                                          THEN 'Google Ads'\n    WHEN lower(utm_source) ILIKE '%sms%'                   THEN 'SMS Campaigns'\n    WHEN lower(utm_source) ILIKE '%chatgpt%'\n      OR lower(utm_source) IN ('perplexity','copilot.com','claude','gemini','bard')\n                                                          THEN 'AI Search'\n    WHEN lower(utm_source) ILIKE '%email%'                 THEN 'Email Campaigns'\n    WHEN lower(utm_source) = 'applemaps'                  THEN 'Apple Maps'\n    WHEN lower(utm_source) IN (\n        'fb','fbads','facebook','facebooks',\n        'facebook_lead_ads','facebook_ads_leads'\n      )\n      OR lower(utm_source) ILIKE '%fb_ads%'                THEN 'Facebook / Meta Ads'\n    WHEN lower(utm_source) IN (\n        'attentive','activecampaign','sqmktg_email',\n        'newsletter','ctct','klarna','trustindex'\n      )\n      OR lower(utm_source) ILIKE '%activecampaign%'        THEN 'Other Marketing Tools'\n    WHEN lower(utm_source) = 'yelp'                       THEN 'Yelp'\n    WHEN lower(utm_source) = 'snapchat'                   THEN 'Snapchat'\n    WHEN lower(utm_source) ILIKE '%bing%'                  THEN 'Bing'\n    WHEN lower(utm_source) = 'direct'                     THEN 'Direct'\n    ELSE 'Unknown / Other'\n  END          AS utm_bucket,\n  count(distinct client_id)     AS enquiries\nFROM website.booking_enquiries\nWHERE entity_id  = '__ENTITY_ID__'\n  AND created_at >= now() - interval '30 days'\nGROUP BY 1\nORDER BY 2 DESC;"},
  {"name":"query4","section":"Funnel & Leads","runnable":true,"deps":[],"sql":"select * from website.booking_enquiries where entity_id = '__ENTITY_ID__'"},
  {"name":"best_rank_query","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"WITH date_sequence AS (\n  SELECT extract_date, row_number() OVER (ORDER BY extract_date DESC) as date_rank\nFROM (SELECT distinct dateval::date as extract_date FROM local_seo.rank \n  WHERE entity_id = '__ENTITY_ID__')\n),\nranked_data AS (\n  SELECT \n    r.keyword,\n    r.dateval::date as extract_date,\n    FLOOR(r.min_rank) as best_rank,\n    ds.date_rank\n  FROM local_seo.rank r\n  JOIN date_sequence ds ON r.dateval::date = ds.extract_date\n  WHERE r.entity_id = '__ENTITY_ID__'\n    AND ds.date_rank <= 10  -- Limit to 10 most recent dates\n),\njoining_ranks AS (\n  SELECT \n    keyword,\n    FLOOR(min_rank) as joining_rank,\n    dateval::date as joining_date\n  FROM local_seo.rank \n  WHERE entity_id = '__ENTITY_ID__'\n    AND dateval::date = (\n      SELECT MIN(dateval::date) \n      FROM local_seo.rank r2 \n      WHERE r2.keyword = local_seo.rank.keyword \n        AND r2.entity_id = '__ENTITY_ID__'\n    )\n)\nSELECT \n  rd.keyword,\n  MAX(CASE WHEN rd.date_rank = 1 THEN rd.best_rank END) as date_1,\n  MAX(CASE WHEN rd.date_rank = 2 THEN rd.best_rank END) as date_2,\n  MAX(CASE WHEN rd.date_rank = 3 THEN rd.best_rank END) as date_3,\n  MAX(CASE WHEN rd.date_rank = 4 THEN rd.best_rank END) as date_4,\n  MAX(CASE WHEN rd.date_rank = 5 THEN rd.best_rank END) as date_5,\n  MAX(CASE WHEN rd.date_rank = 6 THEN rd.best_rank END) as date_6,\n  MAX(CASE WHEN rd.date_rank = 7 THEN rd.best_rank END) as date_7,\n  MAX(CASE WHEN rd.date_rank = 8 THEN rd.best_rank END) as date_8,\n  MAX(CASE WHEN rd.date_rank = 9 THEN rd.best_rank END) as date_9,\n  MAX(CASE WHEN rd.date_rank = 10 THEN rd.best_rank END) as date_10,\n  jr.joining_rank,\n  jr.joining_date\nFROM ranked_data rd\nLEFT JOIN joining_ranks jr ON rd.keyword = jr.keyword\nGROUP BY rd.keyword, jr.joining_rank, jr.joining_date\nORDER BY rd.keyword;"},
  {"name":"date_headers_query","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"SELECT \n  extract_date,\n  date_rank as date_order\nFROM (\n  SELECT extract_date, row_number() OVER (ORDER BY extract_date DESC) as date_rank\nFROM (SELECT distinct dateval::date as extract_date FROM local_seo.rank \n  WHERE entity_id = '__ENTITY_ID__')\n) date_sequence\nWHERE date_rank <= 10\nORDER BY date_rank;"},
  {"name":"keywords","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"SELECT \n    value as keywords\nFROM entities.preferences\nWHERE attribute = 'content.contentPreferences.targetKeywords'  AND entity_id = '__ENTITY_ID__'"},
  {"name":"latest_rank_date","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"SELECT max(created_at::date) as max_date, min(created_at::date) as min_date\nFROM local_seo.rank \nWHERE entity_id = '__ENTITY_ID__'"},
  {"name":"optimized_keywords","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"SELECT keyword, priority FROM entities.keywords\nWHERE priority is not null\nAND is_active = true\nAND entity_id = '__ENTITY_ID__'\nORDER BY 2 ASC"},
  {"name":"query14","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"SELECT count(*) as cnt\nFROM entities.keywords \nWHERE entity_id = '__ENTITY_ID__'"},
  {"name":"RankData","section":"Rankings & SEO","runnable":true,"deps":[],"sql":"WITH base AS (\n    SELECT \n        place_id,\n        keyword,\n        search_volume,\n        created_at,\n        avg_rank,\n        min_rank,\n        rank_delta,\n        RANK() OVER (PARTITION BY entity_id, keyword ORDER BY created_at::date DESC)\n  AS rnk,\n  RANK() OVER (PARTITION BY entity_id, keyword ORDER BY created_at::date ASC)\n  AS rnk1\n    FROM local_seo.rank \n    WHERE entity_id = '__ENTITY_ID__'\n),\nbase2 AS (\n  SELECT \n  keyword, FLOOR(min_rank) as min_rank, FLOOR(avg_rank) as avg_rank, search_volume as volume\nFROM \n    base\nWHERE \n    rnk = 1 \n)\n\nSELECT  \n  base2.keyword,\n  min(base2.min_rank) as min_rank,\n  min(base2.avg_rank) as avg_rank,\n  min(base2.volume) as volume\n  , min(a.joining_rank) as joining_rank\nFROM base2\nJOIN (SELECT \n  keyword, \n  FLOOR(avg_rank) as joining_rank \nFROM base WHERE rnk1 = 1)\n  a ON a.keyword = base2.keyword\nGROUP BY 1"},
  {"name":"cancellation_date","section":"Payments & Billing","runnable":true,"deps":[],"sql":"SELECT\n  (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID AS location_entity_id,\n  (MAX(cs.cancelled_at) AT TIME ZONE 'America/New_York')::DATE AS cancelled_date\n  FROM chargebee.subscriptions cs\n  WHERE cs.deleted IS NOT TRUE\n  AND (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID = '__ENTITY_ID__'::UUID\n  GROUP BY (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID\n  HAVING COUNT(CASE WHEN cs.status IN ('active', 'non_renewing', 'future') THEN 1 END) = 0\n  AND COUNT(CASE WHEN cs.status = 'cancelled' THEN 1 END) > 0\n  AND MAX(cs.cancelled_at) IS NOT NULL"},
  {"name":"CustomerId","section":"Payments & Billing","runnable":true,"deps":[],"sql":"select distinct\n    cs.customer_id\nfrom\n    chargebee.subscriptions cs\nwhere\n    status in ('active', 'future')\n AND (cs.custom_fields::jsonb)->>'cf_entity_id'::text = '__ENTITY_ID__'"},
  {"name":"due_amount","section":"Payments & Billing","runnable":true,"deps":[],"sql":"SELECT (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID AS location_entity_id, COALESCE(ROUND(SUM(ci.amount_due::NUMERIC / 100.0), 2), 0) AS due_amount FROM chargebee.invoices  \n  ci JOIN chargebee.subscriptions cs ON cs.id = ci.subscription_id JOIN entities.entities ee ON ee.entity_id = (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID WHERE ci.deleted IS\n   FALSE AND ci.status IN ('payment_due', 'not_paid') AND cs.deleted IS NOT TRUE AND ee.is_test = 'false' AND (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID = '__ENTITY_ID__'::UUID GROUP BY 1"},
  {"name":"next_billing_date","section":"Payments & Billing","runnable":true,"deps":[],"sql":"SELECT\n\t  (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID AS entity_id, \n\t  (MIN(cs.next_billing_at) AT TIME ZONE 'America/New_York')::DATE AS next_billing_date\n  FROM chargebee.subscriptions cs\n  JOIN entities.entities ee\n  \tON ee.entity_id = (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID\nWHERE cs.status IN ('active', 'non_renewing', 'future')\n  \tAND cs.deleted IS NOT TRUE\n\tAND ee.is_test = 'false' \n\tAND cs.next_billing_at IS NOT NULL\n    AND (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID = '__ENTITY_ID__'::UUID\n\tGROUP BY (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID"},
  {"name":"orphaned_invoices","section":"Payments & Billing","runnable":true,"deps":[],"sql":"WITH\n  -- Get all customer_ids associated with this entity\n  entity_customers AS (\n    SELECT \n      DISTINCT ((cs.custom_fields::jsonb) ->> 'cf_entity_id')::UUID AS entity_id,\n      cs.customer_id\n    FROM\n      chargebee.subscriptions cs\n    WHERE\n      cs.deleted IS FALSE\n      AND (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID = '__ENTITY_ID__'::UUID\n  ),\n\n  -- Get invoices with NO subscription_id for these customers\n  orphan_payments AS (\n    SELECT\n      DISTINCT ci.id AS invoice_id,\n      ec.entity_id,\n      ci.subscription_id,\n      ci.customer_id,\n      ci.status AS invoice_status,\n      li.entity_id AS item_entity_id,\n      li.description,\n      (ci.generated_at AT TIME ZONE 'America/New_York')::DATE AS generated_at,\n      (ci.date AT TIME ZONE 'America/New_York')::DATE AS invoice_date,\n      ci.total::NUMERIC / 100.00 AS amount_raised,\n      li.amount / 100.00 AS item_price,\n      ci.amount_paid::NUMERIC / 100.00 AS amount_paid,\n      (ci.paid_at AT TIME ZONE 'America/New_York')::DATE AS paid_date,\n      ci.amount_due::NUMERIC / 100.00 AS amount_due\n    FROM\n      chargebee.invoices ci\n      JOIN entity_customers ec ON ec.customer_id = ci.customer_id\n      LEFT JOIN chargebee.invoices__line_items li ON li._sdc_source_key_id::TEXT = ci.id::TEXT\n    WHERE\n      ci.subscription_id IS NULL\n  ),\n\n  -- refund Cases\n  refund_amount AS (\n    SELECT\n      cn.reference_invoice_id AS invoice_id,\n      MIN(cn.refunded_at AT TIME ZONE 'America/New_York') AS min_refunded_date,\n      MAX(cn.refunded_at AT TIME ZONE 'America/New_York') AS max_refunded_date,\n      SUM(cn.total::NUMERIC / 100.00) AS total_refund\n    FROM\n      chargebee.credit_notes cn\n    WHERE\n      cn.status IN ('refunded', 'refund_due')\n    GROUP BY\n      1\n  ),\n\n  -- Promotional Credits\n  applied_credits AS (\n    SELECT\n      _sdc_source_key_id AS invoice_id,\n      SUM(applied_amount::NUMERIC / 100.00) AS credits_applied,\n      MAX(applied_at AT TIME ZONE 'America/New_York') AS applied_at\n    FROM\n      chargebee.invoices__applied_credits\n    GROUP BY\n      1\n  ),\n\n  -- Adjusted / Write Off Amount\n  adjusted_amount AS (\n    SELECT\n      cn.reference_invoice_id AS invoice_id,\n      MIN(cn.refunded_at AT TIME ZONE 'America/New_York') AS min_adjusted_date,\n      MAX(cn.refunded_at AT TIME ZONE 'America/New_York') AS max_adjusted_date,\n      SUM(cn.total::NUMERIC / 100.00) AS total_adjusted\n    FROM\n      chargebee.credit_notes cn\n    WHERE\n      cn.status IN ('adjusted')\n    GROUP BY\n      1\n  ),\n\n  -- Promotional invoice discounts\n  invoice_discounts AS (\n    SELECT\n      _sdc_source_key_id AS invoice_id,\n      SUM(amount::NUMERIC / 100.00) AS promotional_credits\n    FROM\n      chargebee.invoices__discounts\n    GROUP BY\n      1\n  )\n\nSELECT\n  DISTINCT p.amount_paid,\n  p.paid_date,\n  p.invoice_date,\n  p.generated_at,\n  p.subscription_id,\n  p.item_entity_id,\n  p.description,\n  p.invoice_id,\n  p.customer_id,\n  CASE\n    WHEN p.amount_raised = (COALESCE(p.amount_paid, 0) + COALESCE(aa.total_adjusted, 0) + COALESCE(ac.credits_applied, 0)) THEN 'paid'\n    ELSE p.invoice_status\n  END AS invoice_status,\n  ROUND(\n    CASE\n      WHEN aa.total_adjusted > 0 THEN p.amount_raised - (aa.total_adjusted + p.amount_paid)\n      ELSE p.amount_due\n    END, 2\n  ) AS amount_due,\n  NULL::TEXT AS customer_auto_collection_status,\n  NULL::TEXT AS subscription_auto_collection_status,\n  'https://public.zoca.com/chargebee/generate/invoice/' || p.invoice_id AS invoice_url,\n  ROUND(aa.total_adjusted, 2) AS total_adjusted,\n  ROUND(ac.credits_applied, 2) AS credits_applied,\n  ROUND(i.promotional_credits, 2) AS promotional_credits,\n  ROUND(ra.total_refund, 2) AS refund_amount\nFROM orphan_payments p\nLEFT JOIN adjusted_amount aa ON aa.invoice_id = p.invoice_id\nLEFT JOIN applied_credits ac ON ac.invoice_id = p.invoice_id\nLEFT JOIN invoice_discounts i ON i.invoice_id = p.invoice_id\nLEFT JOIN refund_amount ra ON ra.invoice_id = p.invoice_id\nORDER BY p.invoice_date DESC"},
  {"name":"payment_invoices","section":"Payments & Billing","runnable":true,"deps":[],"sql":"WITH\n  subs AS (\n    SELECT\n      ((cs.custom_fields::jsonb) ->> 'cf_entity_id')::UUID AS entity_id,\n      cs.id,\n      cs.customer_id\n    FROM\n      chargebee.subscriptions cs\n    WHERE\n      TRUE\n      AND cs.deleted IS NOT TRUE\n      AND (cs.custom_fields::JSONB ->> 'cf_entity_id') IS NOT NULL\n    GROUP BY\n      1,2,3\n  ),\n\n  payments AS (\n    SELECT\n      DISTINCT ci.id AS invoice_id,\n      s.entity_id,\n      s.id subscription_id,\n      s.customer_id,\n      ci.status AS invoice_status,\n      li.entity_id item_entity_id,\n      li.description,\n      (ci.generated_at AT TIME ZONE 'America/New_York')::DATE generated_at,\n      (ci.date AT TIME ZONE 'America/New_York')::DATE AS invoice_date,\n      ci.total::NUMERIC / 100.00 AS amount_raised,\n      li.amount/100.00 item_price,\n      ci.amount_paid::NUMERIC / 100.00 AS amount_paid,\n      (ci.paid_at AT TIME ZONE 'America/New_York')::DATE AS paid_date,\n      ci.amount_due::NUMERIC / 100.00 AS amount_due\n    FROM\n      chargebee.invoices ci\n      JOIN subs s ON s.customer_id = ci.customer_id AND ci.subscription_id = s.id\n      JOIN chargebee.invoices__line_items li ON li._sdc_source_key_id::TEXT = ci.id::TEXT\n    WHERE\n        true\n        AND s.entity_id::TEXT = '__ENTITY_ID__'\n  ),\n\n  -- Refund Cases\n  refund_amount AS (\n    SELECT\n      cn.reference_invoice_id AS invoice_id,\n      MIN(cn.refunded_at AT TIME ZONE 'America/New_York') AS min_refunded_date,\n      MAX(cn.refunded_at AT TIME ZONE 'America/New_York') AS max_refunded_date,\n      SUM(cn.total::NUMERIC / 100.00) AS total_refund\n    FROM\n      chargebee.credit_notes cn\n    WHERE\n      cn.status IN ('refunded', 'refund_due')\n    GROUP BY\n      1\n  ),\n\n  -- Promotional Credits\n  applied_credits AS (\n    SELECT\n      _sdc_source_key_id invoice_id,\n      SUM(applied_amount::NUMERIC / 100.00) credits_applied,\n      MAX(applied_at AT TIME ZONE 'America/New_York') applied_at\n    FROM\n      chargebee.invoices__applied_credits\n    GROUP BY\n      1\n  ),\n\n  -- Adjusted / Write Off Amount\n  adjusted_amount AS (\n    SELECT\n      cn.reference_invoice_id AS invoice_id,\n      MIN(cn.refunded_at AT TIME ZONE 'America/New_York') AS min_adjusted_date,\n      MAX(cn.refunded_at AT TIME ZONE 'America/New_York') AS max_adjusted_date,\n      SUM(cn.total::NUMERIC / 100.00) AS total_adjusted\n    FROM\n      chargebee.credit_notes cn\n    WHERE\n      cn.status IN ('adjusted')\n    GROUP BY\n      1\n  ),\n\n  -- Promotional Invoice Discounts\n  invoice_discounts AS (\n    SELECT\n      _sdc_source_key_id invoice_id,\n      SUM(amount::NUMERIC / 100.00) promotional_credits\n    FROM\n      chargebee.invoices__discounts\n    GROUP BY\n      1\n  ),\n\n  auto_collection_data AS (\n    SELECT\n        DISTINCT (cs.custom_fields::JSONB ->> 'cf_entity_id')::UUID entity_id,\n        cs.id subscription_id,\n        CASE WHEN cs.auto_collection IS NULL THEN cc.auto_collection ELSE cs.auto_collection END AS subscription_auto_collection_status,\n        cc.id customer_id,\n        cc.auto_collection customer_auto_collection_status\n    FROM chargebee.subscriptions cs\n    LEFT JOIN chargebee.customers cc\n        ON cs.customer_id = cc.id\n    LEFT JOIN entities.entities ee ON ee.entity_id = (cs.custom_fields::jsonb->>'cf_entity_id')::uuid AND ee.is_test = 'false'\n    WHERE cs.status IN ('active', 'future')\n  ),\n\n  -- NEW: Get Stripe charge_id per invoice (latest transaction)\n  stripe_id AS (\n    SELECT\n      invoice_id,\n      id_at_gateway AS charge_id\n    FROM (\n      SELECT\n        DISTINCT ctli.invoice_id,\n        ct.id_at_gateway,\n        RANK() OVER (PARTITION BY ctli.invoice_id ORDER BY ct.date DESC) AS rk\n      FROM\n        chargebee.transactions__linked_invoices ctli\n        INNER JOIN chargebee.transactions ct\n          ON ctli._sdc_source_key_id = ct.id\n      WHERE\n        ct.deleted IS NOT TRUE\n    )\n    WHERE rk = 1\n  ),\n\n  -- NEW: Dispute amounts from Stripe\n  disputes_data AS (\n    SELECT\n      DISTINCT charge,\n      amount / 100.00 AS dispute_amount,\n      created AT TIME ZONE 'America/New_York' AS disputed_created_date\n    FROM\n      stripe_stitch.disputes\n    WHERE\n      status NOT IN ('won')\n  )\n\nSELECT\n    DISTINCT p.amount_paid,\n    p.paid_date,\n    p.invoice_date,\n    p.generated_at,\n    p.subscription_id,\n    p.item_entity_id,\n    p.description,\n    p.invoice_id,\n    p.customer_id,\n    CASE WHEN p.amount_raised = (COALESCE(p.amount_paid,0) + COALESCE(aa.total_adjusted,0) + COALESCE(ac.credits_applied,0)) THEN 'paid'\n    ELSE p.invoice_status END invoice_status,\n    ROUND(CASE\n      WHEN total_adjusted > 0 THEN amount_raised - (total_adjusted + amount_paid)\n      ELSE amount_due\n    END, 2) amount_due,\n    acd.customer_auto_collection_status,\n    acd.subscription_auto_collection_status,\n    'https://public.zoca.com/chargebee/generate/invoice/' || p.invoice_id invoice_url,\n    ROUND(aa.total_adjusted, 2) total_adjusted,\n    ROUND(ac.credits_applied, 2) credits_applied,\n    ROUND(i.promotional_credits, 2) promotional_credits,\n    ROUND(ra.total_refund, 2) refund_amount,\n    ROUND(COALESCE(dd.dispute_amount, 0), 2) AS dispute_amount,\n    dd.disputed_created_date\nFROM payments p\nLEFT JOIN adjusted_amount aa ON aa.invoice_id = p.invoice_id\nLEFT JOIN applied_credits ac ON ac.invoice_id = p.invoice_id\nLEFT JOIN invoice_discounts i ON i.invoice_id = p.invoice_id\nLEFT JOIN refund_amount ra ON ra.invoice_id = p.invoice_id\nLEFT JOIN auto_collection_data acd ON acd.customer_id = p.customer_id AND acd.subscription_id = p.subscription_id\nLEFT JOIN stripe_id si ON si.invoice_id = p.invoice_id\nLEFT JOIN disputes_data dd ON dd.charge = si.charge_id\nORDER BY p.invoice_date DESC;"},
  {"name":"paymentRelatedLinks","section":"Payments & Billing","runnable":true,"deps":[],"sql":"select\n    'https://public.zoca.com/chargebee/missed/payment/'||entity_id missed_payment,\n    'https://public.zoca.com/chargebee/update/payment/method/'||entity_id payment_method_update\nfrom\n  entities.entities\nwhere\n  entity_id='__ENTITY_ID__'"},
  {"name":"payments","section":"Payments & Billing","runnable":true,"deps":[],"sql":"SELECT\n  \"source\".\"location_entity_id\" AS \"location_entity_id\",\n  \"source\".\"chargebee_customer_id\" AS \"chargebee_customer_id\",\n  \"source\".\"location_entity_name\" AS \"location_entity_name\",\n  \"source\".\"local_seo_monthly_amount\" AS \"local_seo_monthly_amount\",\n  \"source\".\"website_monthly_amount\" AS \"website_monthly_amount\",\n  \"source\".\"campaigns_monthly_amount\" AS \"campaigns_monthly_amount\",\n  \"source\".\"social_media_monthly_amount\" AS \"social_media_monthly_amount\",\n  \"source\".\"ads_budget_monthly_amount\" AS \"ads_budget_monthly_amount\",\n  \"source\".\"ads_management_monthly_amount\" AS \"ads_management_monthly_amount\",\n  \"source\".\"lead_to_booking_monthly_amount\" AS \"lead_to_booking_monthly_amount\",\n  \"source\".\"domain_monthly_amount\" AS \"domain_monthly_amount\",\n  \"source\".\"front_desk_amount\" AS \"Front_Desk_amount\",\n  \"source\".\"paid_status\" AS \"paid_status\"\n  FROM\n  (\n  WITH subscription_items AS (\n  SELECT\n  ssi._sdc_source_key_id,\n  ssi.item_price_id,\n  ssi.amount_in_decimal :: numeric,\n  sub.billing_period_unit,\n  sub.billing_period\n  FROM\n  chargebee.subscriptions__subscription_items AS ssi\n  JOIN (\n  SELECT\n  cs.id,\n  ((cs.custom_fields :: jsonb) ->> 'cf_entity_id') :: uuid AS entity_id,\n  cs.billing_period_unit,\n  cs.billing_period\n  FROM\n  chargebee.subscriptions AS cs\n  WHERE\n  cs.status IN ('active', 'future', 'non_renewing') AND cs.deleted = false\n  ) AS sub ON sub.id = ssi._sdc_source_key_id\n  WHERE\n  ssi.item_type = 'addon'\n  ),\n  adjusted_amounts AS (\n  SELECT\n  _sdc_source_key_id,\n  item_price_id,\n  CASE\n  WHEN billing_period_unit = 'week' THEN amount_in_decimal * (4 :: numeric / billing_period)\n  WHEN billing_period_unit = 'month' THEN amount_in_decimal / billing_period\n  WHEN billing_period_unit = 'year' THEN amount_in_decimal / (12::numeric * billing_period)\n  ELSE amount_in_decimal\n  END AS adjusted_amount\n  FROM\n  subscription_items\n  ),\n  subscription_data AS (\n  SELECT\n  cs.id,\n  cs.customer_id,\n  ((cs.custom_fields :: jsonb) ->> 'cf_entity_id') :: uuid AS entity_id,\n  cs.status\n  FROM\n  chargebee.subscriptions AS cs\n  WHERE\n  cs.status IN ('active', 'future', 'non_renewing')\n  )\n  SELECT\n  loc.entity_id AS location_entity_id,\n  sub.customer_id chargebee_customer_id,\n  loc.name AS location_entity_name,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Local-SEO-%' OR ai.item_price_id LIKE '%Discovery%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS local_seo_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Website-%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS website_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Campaigns-%' OR ai.item_price_id ILIKE 'Loyalty%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS campaigns_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Social-%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS social_media_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE '%budget%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS ads_budget_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Ads-Management-%' OR ai.item_price_id ILIKE 'Ads-Agent-%' AND NOT ai.item_price_id ILIKE '%budget%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS ads_management_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Lead-to-Booking-%' OR ai.item_price_id LIKE '%Win%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS lead_to_booking_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE 'Domain-%' THEN ai.adjusted_amount\n  ELSE 0 :: numeric\n  END\n  ) AS domain_monthly_amount,\n  MAX(\n  CASE\n  WHEN ai.item_price_id ILIKE '%frontdesk%' THEN ai.adjusted_amount\n  ELSE 0:: numeric\n  END\n  ) AS front_desk_amount,\n  CASE\n  WHEN MAX(CASE WHEN sub.status IN ('active', 'future') THEN 1 ELSE 0 END) = 1 THEN 'Paid'\n  WHEN MAX(CASE WHEN sub.status = 'non_renewing' THEN 1 ELSE 0 END) = 1 THEN 'Non-renewing'\n  ELSE 'Unpaid'\n  END AS paid_status\n  FROM\n  entities.locations AS loc\n  JOIN subscription_data sub ON sub.entity_id = loc.entity_id\n  JOIN adjusted_amounts AS ai ON ai._sdc_source_key_id = sub.id\n  GROUP BY\n  1,\n  2,\n  3\n  ORDER BY\n  3\n  ) AS \"source\"\n  WHERE \"source\".\"location_entity_id\" = '__ENTITY_ID__'::UUID"},
  {"name":"refunds","section":"Payments & Billing","runnable":true,"deps":[],"sql":"WITH subs AS (\n    SELECT\n        ((cs.custom_fields::jsonb) ->> 'cf_entity_id')::uuid AS entity_id,\n        cs.id,\n        cs.customer_id,\n        min(started_at) as subscription_creation_date\n    FROM chargebee.subscriptions cs\n    GROUP BY 1, 2, 3\n),\nrefunds AS (\n    SELECT \n        cn.*,\n        li.amount as line_item_amount,\n        li.description as line_item_description,\n        li.entity_id as line_item_entity_id,\n        li.entity_type as line_item_entity_type,\n        lr.applied_amount as refund_applied_amount,\n        lr.applied_at as refund_applied_at,\n        lr.txn_amount as refund_txn_amount,\n        lr.txn_date as refund_txn_date,\n        lr.txn_id as refund_txn_id,\n        lr.txn_status as refund_txn_status,\n        cn.status as credit_note_status,\n        cn.id as credit_note_id,\n        subs.entity_id as subscription_entity_id\n    FROM chargebee.credit_notes cn\n    JOIN subs ON subs.id = cn.subscription_id\n    LEFT JOIN chargebee.credit_notes__line_items li ON li._sdc_source_key_id::text = cn.id::text\n    LEFT JOIN chargebee.credit_notes__linked_refunds lr ON lr._sdc_source_key_id::text = cn.id::text\n    WHERE \n      true\n      AND cn.deleted is false\n      AND subs.entity_id = '__ENTITY_ID__'\n)\nSELECT \n    credit_note_id,\n    credit_note_status,\n    ROUND(total/100.0, 2) as total_credit_amount,\n    ROUND(amount_refunded/100.0, 2) as amount_refunded,\n    ROUND(amount_allocated/100.0, 2) as amount_allocated,\n    ROUND(amount_available/100.0, 2) as amount_available,\n    date::date as credit_note_date,\n    refunded_at::date as refunded_date,\n    generated_at::date as generated_date,\n    reason_code,\n    create_reason_code,\n    reference_invoice_id,\n    type as credit_note_type,\n    ROUND(COALESCE(line_item_amount, 0)/100.0, 2) as line_item_amount,\n    line_item_description,\n    line_item_entity_type,\n    subscription_entity_id,\n    -- Linked refund details\n    refund_txn_id as refund_transaction_id,\n    ROUND(COALESCE(refund_txn_amount, 0)/100.0, 2) as refund_transaction_amount,\n    refund_txn_date::date as refund_transaction_date,\n    refund_txn_status as refund_transaction_status,\n    ROUND(COALESCE(refund_applied_amount, 0)/100.0, 2) as refund_applied_amount,\n    refund_applied_at::date as refund_applied_date\nFROM refunds \nORDER BY date DESC, generated_at DESC"},
  {"name":"trial_data","section":"Payments & Billing","runnable":true,"deps":[],"sql":"SELECT \n  trial.attributes.created_at::date as trial_start_date\nFROM trial.attributes\nLEFT JOIN chrone_mapping.uid_location ON chrone_mapping.uid_location.uid = trial.attributes.entity_id\nWHERE chrone_mapping.uid_location.is_migrated IS NULL AND entity_id = '__ENTITY_ID__'"},
  {"name":"booking_platform_link","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT booking_link, booking_platform FROM website.locations where location_id = '__ENTITY_ID__'"},
  {"name":"bookings_by_creator_type","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT \n  bi.created_by_type,\n  COUNT(DISTINCT b.id) AS booking_count\nFROM \"scheduling\".\"bookings\" b\nLEFT JOIN \"scheduling\".\"booking_items\" bi ON b.id = bi.booking_id\nWHERE b.entity_id = '__ENTITY_ID__'\n  AND ((b.attributes #>> array['migration_source']::text[])::text IS NULL\n        OR (b.attributes #>> array['migration_source']::text[])::text = '')\nGROUP BY bi.created_by_type\nORDER BY bi.created_by_type ASC"},
  {"name":"bookings_by_status","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT\n  b.status,\n  COUNT(DISTINCT b.id) AS booking_count\nFROM\n  \"scheduling\".\"bookings\" b\nWHERE\n  b.entity_id = '__ENTITY_ID__'\n  AND (\n    (b.attributes #>> array['migration_source']::text[])::text IS NULL\n    OR (b.attributes #>> array['migration_source']::text[])::text = ''\n  )\nGROUP BY\n  b.status\nORDER BY\n  b.status ASC"},
  {"name":"lead_to_booking_scheduling","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"WITH weeks AS (\n    SELECT generate_series(\n        DATE_TRUNC('week', CURRENT_DATE - INTERVAL '3 months'),\n        DATE_TRUNC('week', CURRENT_DATE),\n        '1 week'::interval\n    )::date AS week_start\n),\n\nleads_weekly AS (\n    SELECT\n        DATE_TRUNC('week', wbe.created_at)::date AS week_start,\n        COUNT(DISTINCT wbe.client_id) AS unique_leads\n    FROM website.booking_enquiries wbe\n    WHERE wbe.entity_id = '__ENTITY_ID__'::uuid\n      AND wbe.is_test_lead = false\n      AND wbe.created_at >= DATE_TRUNC('week', CURRENT_DATE - INTERVAL '3 months')\n    GROUP BY 1\n),\n\nbookings_weekly AS (\n    SELECT\n        DATE_TRUNC('week', b.created_at)::date AS week_start,\n        COUNT(DISTINCT b.client_id) AS unique_bookings\n    FROM scheduling.bookings b\n    WHERE b.entity_id = '__ENTITY_ID__'::uuid\n      AND b.created_at >= DATE_TRUNC('week', CURRENT_DATE - INTERVAL '3 months')\n    GROUP BY 1\n)\n\nSELECT\n    w.week_start,\n    TO_CHAR(w.week_start, 'DD Mon YYYY') AS week_label,\n    COALESCE(l.unique_leads, 0) AS unique_leads,\n    COALESCE(bk.unique_bookings, 0) AS unique_bookings\nFROM weeks w\nLEFT JOIN leads_weekly l ON l.week_start = w.week_start\nLEFT JOIN bookings_weekly bk ON bk.week_start = w.week_start\nORDER BY w.week_start ASC;"},
  {"name":"query19","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT sse.entity_id, sse.service_id, ss.name, ss.description, ss.duration, ss.price, sc.name as category_name, sc.description as category_description\nFROM services.services_entities sse \nJOIN services.services ss ON ss.id::text = sse.service_id::text\nJOIN services.services_categories ssc ON ssc.service_id::text = ss.id::text\nJOIN services.categories sc ON sc.id::text = ssc.category_id::text\nWHERE ss.is_active = true AND ss.is_deleted = false AND sc.is_deleted = false AND ssc.is_deleted = false AND sse.is_deleted = false AND sc.is_active = true AND sse.entity_id = '__ENTITY_ID__'"},
  {"name":"query37","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT\n  CASE WHEN EXISTS (\n    SELECT 1 FROM scheduling.onboarding so\n    WHERE so.location_entity_id = '__ENTITY_ID__'\n      AND so.is_website_flipped = true\n  ) THEN 'Yes' ELSE 'No' END AS is_active_on_scheduling,\n  CASE WHEN EXISTS (\n    SELECT 1 FROM entities.preferences ep\n    WHERE ep.entity_id = '__ENTITY_ID__'\n      AND ep.attribute = 'website.callNow.buttonText'\n      AND ep.value = 'Call Us (24x7)'\n  ) THEN 'Yes' ELSE 'No' END AS is_call_cta_enabled;"},
  {"name":"scheduling_status","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT\n  CASE\n    WHEN EXISTS (\n      SELECT 1\n      FROM \"entities\".\"product_entities\"\n      WHERE \"entity_id\" ='__ENTITY_ID__'\n        AND \"product_id\" = 10\n        AND \"is_active\" = true\n    )\n    THEN 'Active'\n    ELSE 'Not Active'\n  END AS \"status\""},
  {"name":"total_booking_count","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"SELECT COUNT(DISTINCT b.id) AS total_bookings\nFROM \"scheduling\".\"bookings\" b\nWHERE b.entity_id = '__ENTITY_ID__'\n  AND ((b.attributes #>> array['migration_source']::text[])::text IS NULL\n        OR (b.attributes #>> array['migration_source']::text[])::text = '')"},
  {"name":"weekly_conversion_rate","section":"Scheduling & Bookings","runnable":true,"deps":[],"sql":"WITH filtered_enquiries AS (\n    SELECT DISTINCT be.id, be.created_at\n    FROM website.booking_enquiries be\n    JOIN scheduling.onboarding so ON so.location_entity_id = be.entity_id AND is_opted_in = 'true' AND is_website_flipped = 'true'\n    JOIN entities.locations l ON be.entity_id = l.entity_id\n    JOIN entities.entities e ON l.entity_id = e.entity_id\n    WHERE\n      be.entity_id = '__ENTITY_ID__'\n    AND be.is_test_lead = FALSE\n          AND e.is_test = FALSE\n          AND l.name IS NOT NULL\n          AND be.attributes->>'form_type' = 'scheduling_form'\n          AND be.created_at >= so.opted_in_at\n      ),\n      carts_with_services AS (\n        SELECT DISTINCT c.enquiry_id, c.booking_id\n        FROM scheduling.carts c\n        JOIN scheduling.cart_items ci ON ci.cart_id = c.id\n      )\n      SELECT\n        DATE_TRUNC('week', fe.created_at)::date AS week_starting,\n        ROUND(\n          COUNT(DISTINCT CASE WHEN cws.booking_id IS NOT NULL THEN cws.enquiry_id END)::decimal\n          / NULLIF(COUNT(DISTINCT fe.id), 0) * 100, 1\n        ) AS overall_conversion_pct\n      FROM filtered_enquiries fe\nLEFT JOIN carts_with_services cws ON fe.id = cws.enquiry_id\nGROUP BY DATE_TRUNC('week', fe.created_at)\nORDER BY week_starting"},
  {"name":"conversation_details","section":"Calls & Chatbot","runnable":true,"deps":[],"sql":"WITH locations AS (\n    SELECT\n        DISTINCT cm.conversation_id,\n        eer.entity_2_id AS location_entity_id,\n        CASE\n            WHEN amm.entity_id IS NOT NULL \n                THEN eem1.first_name || ' ' || eem1.last_name\n            ELSE eem2.first_name || ' ' || eem2.last_name\n        END AS rm_name,\n\t\tCASE \n\t\t\tWHEN amm.entity_id is not null \n\t\t\t\tTHEN eem1.type \n\t\t\tELSE eem2.type \n\t\tEND AS rm_type\n    FROM chat.conversation_members cm\n    JOIN chat.members m\n        ON cm.member_id = m.member_id\n    JOIN entities.users eu\n        ON eu.entity_id = m.member_id\n    JOIN entities.entity_relationships eer\n        ON eer.entity_1_id = eu.entity_id\n    JOIN chargebee.subscriptions cs\n        ON eer.entity_2_id = (cs.custom_fields::json->>'cf_entity_id')::uuid\n    LEFT JOIN cx.am_mapping amm\n        ON amm.entity_id = eer.entity_2_id\n    LEFT JOIN cx.ae_mapping aem\n        ON aem.entity_id = eer.entity_2_id\n    LEFT JOIN entities.employees eem1\n        ON eem1.entity_id = amm.am_entity_id\n    LEFT JOIN entities.employees eem2\n        ON eem2.entity_id = aem.ae_entity_id\n    WHERE\n        m.type = 'User'\n        AND cm.is_active = TRUE\n        AND eer.relationship_type = 'HAS_LOCATION'\n\t\tAND eer.entity_2_id = '__ENTITY_ID__'\n),\nteam_members AS (\n    SELECT\n        cm.conversation_id,\n        STRING_AGG(m.name, ', ') AS team_members_list\n    FROM chat.conversation_members cm\n    JOIN chat.members m\n        ON cm.member_id = m.member_id\n    WHERE\n        m.type = 'Team Member'\n        AND cm.is_active = TRUE\n    GROUP BY\n        cm.conversation_id\n),\nlast_messages AS (\n    SELECT\n        m.conversation_id,\n        m.created_at AS message_time,\n        mem.name AS sender_name,\n        ROW_NUMBER() OVER (\n            PARTITION BY m.conversation_id\n            ORDER BY m.created_at DESC\n        ) AS rn\n    FROM chat.messages m\n    JOIN chat.members mem\n        ON m.from = mem.member_id\n    LEFT JOIN entities.entities ee\n        ON ee.entity_id = mem.member_id\n    WHERE\n        m.is_deleted = FALSE\n        AND NOT COALESCE(ee.is_test, FALSE)\n)\nSELECT\n    -- l.location_entity_id,\n    c.id AS conversation_id,\n    c.name AS conversation_name,\n    l.rm_name,\n\tl.rm_type,\n    COALESCE(tm.team_members_list, 'No team members') AS team_members,\n    CASE \n        WHEN l.rm_name IS NOT NULL \n            AND (\n                COALESCE(tm.team_members_list, '') ILIKE '%' || SPLIT_PART(l.rm_name, ' ', 1) || '%'\n                OR COALESCE(tm.team_members_list, '') ILIKE '%' || SPLIT_PART(l.rm_name, ' ', 2) || '%'\n            )\n        THEN TRUE \n        ELSE FALSE \n    END AS rm_in_team,\n    lm.sender_name,\n    lm.message_time AS final_message_time\nFROM locations l\nJOIN chat.conversations c\n    ON c.id = l.conversation_id\nLEFT JOIN team_members tm\n    ON c.id = tm.conversation_id\nLEFT JOIN last_messages lm\n    ON c.id = lm.conversation_id AND lm.rn = 1\nWHERE\n    c.is_active = TRUE\nORDER BY\n    l.location_entity_id, l.rm_name, c.name;"},
  {"name":"csat_submitted","section":"Calls & Chatbot","runnable":true,"deps":[],"sql":"WITH base_sub AS (\n  SELECT hidden::json->>'entity_id', landing_id, platform , _sdc_form_id as form_id, landed_at, submitted_at\n  FROM csat_typeform_stitch.submitted_landings \n  WHERE hidden::json->>'entity_id' = '__ENTITY_ID__'\n),\n\n-- base_unsub AS (\n--   SELECT hidden::json->>'entity_id', landing_id, platform , _sdc_form_id as form_id, landed_at\n--   FROM csat_typeform_stitch.unsubmitted_landings\n  \n-- ),\nforms AS (\n  SELECT id, title, type FROM csat_typeform_stitch.forms \n),\nquestions AS (\n  SELECT question_id, form_id, type, title FROM csat_typeform_stitch.questions\n)\n\n\nSELECT \n  bs.landed_at,\n  bs.platform,\n  f.title as form_type,\n  f.type as form_type,\n  q.title as question,\n  q.type as question_type,\n  a.answer\nFROM base_sub bs \nJOIN forms f ON bs.form_id = f.id\nJOIN questions q ON q.form_id = f.id \nLEFT JOIN csat_typeform_stitch.answers a ON a.question_id = q.question_id AND a.landing_id = bs.landing_id"},
  {"name":"query35","section":"Calls & Chatbot","runnable":true,"deps":[],"sql":"SELECT COUNT(*) AS total_calls\nFROM chatbot.transcript_mapping\nWHERE entity_id = '__ENTITY_ID__'\n  AND is_test = false;"},
  {"name":"query36","section":"Calls & Chatbot","runnable":true,"deps":[],"sql":"WITH onboard AS (\n  SELECT completed_at\n  FROM l2b.win_onboarding_status\n  WHERE entity_id = '__ENTITY_ID__'\n    AND status = 'completed'\n  ORDER BY completed_at DESC\n  LIMIT 1\n)\nSELECT\n  DATE_TRUNC('week', ctm.start_time)::date AS week,\n  COUNT(*) AS call_count\nFROM chatbot.transcript_mapping ctm\nCROSS JOIN onboard o\nWHERE ctm.entity_id = '__ENTITY_ID__'\n  AND ctm.is_test = false\n  AND ctm.start_time >= o.completed_at\nGROUP BY 1\nORDER BY 1;"},
  {"name":"query38","section":"Calls & Chatbot","runnable":true,"deps":[],"sql":"SELECT \n  l2bcc.action AS action,\n  COUNT(*) AS count\nFROM l2b.call_callbacks l2bcc\nLEFT JOIN chatbot.transcript_mapping ctm ON l2bcc.call_id = ctm.call_id\nWHERE l2bcc.entity_id = '__ENTITY_ID__'\n  AND ctm.is_test = false\nGROUP BY l2bcc.action\nORDER BY count DESC;"},
  {"name":"query39","section":"Calls & Chatbot","runnable":true,"deps":[],"sql":"SELECT \n  DATE_TRUNC('week', cc.created_at)::date AS week,\n  COUNT(*) AS total_tasks,\n  COUNT(*) FILTER (WHERE cc.status = 'completed') AS completed,\n  COUNT(*) FILTER (WHERE cc.status = 'cancelled') AS cancelled,\n  COUNT(*) FILTER (WHERE cc.status = 'pending') AS pending,\n  ROUND(100.0 * COUNT(*) FILTER (WHERE cc.status != 'pending') / NULLIF(COUNT(*), 0), 1) AS resolution_rate_pct\nFROM l2b.call_callbacks cc\nLEFT JOIN chatbot.transcript_mapping ctm ON cc.call_id = ctm.call_id\nWHERE cc.entity_id = '__ENTITY_ID__'\n  AND ctm.is_test = false\nGROUP BY 1\nORDER BY 1;"},
  {"name":"appmetrics","section":"App Engagement","runnable":true,"deps":[],"sql":"SELECT \n    date_trunc('week', time::date)::date AS week,\n    SUM(CASE WHEN event = 'Home-View-Home' THEN 1 ELSE 0 END)  AS app_open,\n    SUM(CASE WHEN event = 'Leads-View-Home' THEN 1 ELSE 0 END) AS leads_open,\n    SUM(CASE WHEN event = 'Review-View-Home' THEN 1 ELSE 0 END) AS reviews_open,\n    SUM(CASE WHEN event = 'Photos-View-Home' THEN 1 ELSE 0 END) AS photos_open,\n    SUM(CASE WHEN event = 'ServiceManager-View-Home' THEN 1 ELSE 0 END) AS servicemanager_open,\n    SUM(CASE WHEN event = 'Chat-View-Home' THEN 1 ELSE 0 END) AS chat_open,\n    SUM(CASE WHEN event = 'Website-View-Home' THEN 1 ELSE 0 END) AS website_open,\n    SUM(CASE WHEN event = 'GooglePost-Click-ViewAllPostsOnGoogle' THEN 1 ELSE 0 END) AS googlepost_open,\n    SUM(CASE WHEN event = 'Photos-Upload-UploadFromGallery' THEN 1 ELSE 0 END) AS upload_photos,\n    SUM(CASE WHEN \n        event = 'Review-Click-SendInviteSingle' \n        OR (event = 'Review-Action-InviteMultipleContacts' AND action = 'send_invite')\n    THEN 1 ELSE 0 END) AS review_invite,\n    SUM(CASE WHEN \n        event = 'Leads-Click-ChatMessage' \n        OR event = 'Leads-Click-DetailChat' \n        OR event = 'Leads-Click-ChatCall' \n        OR event = 'Leads-Click-DetailCall' \n    THEN 1 ELSE 0 END) AS lead_contacted\n\nFROM mixpanelzocaappdata.export\nWHERE \"locationEntityId\" = '__ENTITY_ID__'\nGROUP BY 1"},
  {"name":"AE_AM","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"WITH AM AS (\n  SELECT\n  am.entity_id AS location_entity_id,\n  am.am_entity_id,\n  CONCAT(emp.first_name, ' ', emp.last_name) AS account_manager_name,\n  emp.type AS account_manager_type\n  FROM cx.am_mapping am\n  LEFT JOIN entities.employees emp ON emp.entity_id = am.am_entity_id\n  WHERE am.entity_id = '__ENTITY_ID__'::UUID\n  ),\n  AE_main AS (\n  SELECT\n  ae.entity_id AS location_entity_id,\n  ae.ae_entity_id,\n  CONCAT(emp.first_name, ' ', emp.last_name) AS account_executive_name,\n  emp.type AS account_executive_type\n  FROM cx.ae_mapping ae\n  LEFT JOIN entities.employees emp ON emp.entity_id = ae.ae_entity_id\n  WHERE ae.entity_id = '__ENTITY_ID__'::UUID\n  ),\n  AE_fallback AS (\n  SELECT\n  sd.entity_id AS location_entity_id,\n  CONCAT(emp.first_name, ' ', emp.last_name) AS account_executive_name,\n  ROW_NUMBER() OVER (PARTITION BY sd.entity_id ORDER BY start_time DESC) AS demo_rank\n  FROM sales.demo_call_tracker sd\n  LEFT JOIN entities.employees emp ON emp.entity_id::text = sd.demo_taker_entity_id_actual::text\n  WHERE sd.entity_id = '__ENTITY_ID__'::UUID\n  AND is_attended = 'attended'\n  )\n  SELECT 'Account Manager' AS relation, am.account_manager_name AS employee\n  FROM AM am\n  UNION ALL\n  SELECT 'Account Executive' AS relation,\n  COALESCE(ae_main.account_executive_name, ae_fb.account_executive_name) AS employee\n  FROM AE_main ae_main\n  FULL OUTER JOIN AE_fallback ae_fb\n  ON ae_main.location_entity_id = ae_fb.location_entity_id\n  AND ae_fb.demo_rank = 1\n  WHERE ae_main.ae_entity_id IS NOT NULL\n  OR ae_fb.demo_rank = 1;"},
  {"name":"businesses","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"WITH businesses AS (\n  SELECT er.entity_1_id AS user_entity_id\n  FROM entities.entity_relationships er\n  WHERE er.entity_2_id::text = '__ENTITY_ID__'\n  UNION ALL \n  SELECT er.entity_2_id AS user_entity_id\n  FROM entities.entity_relationships er\n  WHERE er.entity_1_id::text = '__ENTITY_ID__'\n)\n\nSELECT \n  entity_id AS business_entity_id,\n  eu.title as business_name,\n  eu.created_at as business_created_at\nFROM entities.businesses eu\nJOIN businesses ON businesses.user_entity_id = eu.entity_id"},
  {"name":"getAMCalendlyLink","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"select\n  cam.entity_id,\n  attributes->'calendly_config'->'checkInUrl' calendly_url\nfrom\n  entities.employees eem\n  inner join cx.am_mapping cam\n  on eem.entity_id = cam.am_entity_id\nwhere\n  type in ('AM','CX_LEAD')\n  and is_active\n  and cam.entity_id='__ENTITY_ID__'"},
  {"name":"getDealandPersonId","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"select distinct\n  el.entity_id,\n  el.name,\n  pd.deal_id,\n  pd.person_id\nfrom\n  entities.locations el\n  left join (select entity_id, max(id) deal_id, max(person_id) person_id from pipedrive.deals where person_id is not null group by 1) pd\n  on el.entity_id = pd.entity_id\nwhere\n  el.entity_id='__ENTITY_ID__'"},
  {"name":"getPreRead","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"SELECT \n  hb.entity_id, \n  cam.am_entity_id, \n  eem.first_name || ' ' || eem.last_name AS am_name,\n\n  -- ============ TOP-LEVEL FIELDS ============\n  hb.brief_summary->>'nextStepsForAm' AS next_steps_for_am,\n  hb.brief_summary->>'issuesGapsNoted' AS issues_gaps_noted,\n  hb.brief_summary->>'clientPainPoints' AS client_pain_points,\n  hb.brief_summary->>'upsellCrosssellOpportunities' AS upsell_opportunities,\n\n  -- ============ RISK ASSESSMENT ============\n  hb.brief_summary->'riskAssessment'->>'businessRisk' AS business_risk,\n  hb.brief_summary->'riskAssessment'->>'commitmentRisk' AS commitment_risk,\n  hb.brief_summary->'riskAssessment'->>'technologyRisk' AS technology_risk,\n  hb.brief_summary->'riskAssessment'->>'expectationRisk' AS expectation_risk,\n\n  -- ============ BASIC BUSINESS PROFILE ============\n  hb.brief_summary->'basicBusinessProfile'->>'clientName' AS client_name,\n  hb.brief_summary->'basicBusinessProfile'->>'businessName' AS business_name,\n  hb.brief_summary->'basicBusinessProfile'->>'businessType' AS business_type,\n  hb.brief_summary->'basicBusinessProfile'->>'preferredName' AS preferred_name,\n  hb.brief_summary->'basicBusinessProfile'->>'monthlyRevenue' AS monthly_revenue,\n  hb.brief_summary->'basicBusinessProfile'->>'yearsInBusiness' AS years_in_business,\n  hb.brief_summary->'basicBusinessProfile'->>'businessLocation' AS business_location,\n  hb.brief_summary->'basicBusinessProfile'->>'customerBaseSize' AS customer_base_size,\n  hb.brief_summary->'basicBusinessProfile'->>'fullTimePartTime' AS full_time_part_time,\n  hb.brief_summary->'basicBusinessProfile'->>'weeklyClientVolume' AS weekly_client_volume,\n  hb.brief_summary->'basicBusinessProfile'->>'recentBusinessChanges' AS recent_business_changes,\n  hb.brief_summary->'basicBusinessProfile'->>'previousMarketingSpend' AS previous_marketing_spend,\n  hb.brief_summary->'basicBusinessProfile'->>'currentCustomerAcquisition' AS current_customer_acquisition,\n\n  -- ============ KEY FEATURES DISCUSSED ============\n  hb.brief_summary->'keyFeaturesDiscussed'->>'appComfortLevel' AS app_comfort_level,\n  hb.brief_summary->'keyFeaturesDiscussed'->>'multiLocationBusiness' AS multi_location_business,\n  hb.brief_summary->'keyFeaturesDiscussed'->'productFeatures' AS product_features_array, -- Returns full array\n\n  -- ============ EXPECTATIONS AND PROMISES ============\n  hb.brief_summary->'expectationsAndPromises'->>'allCommitmentsMade' AS all_commitments_made,\n  \n  -- Lead Generation Expectations\n  hb.brief_summary->'expectationsAndPromises'->'leadGenerationExpectations'->>'quantityPromised' AS quantity_promised,\n  hb.brief_summary->'expectationsAndPromises'->'leadGenerationExpectations'->>'specificPromises' AS specific_promises,\n  hb.brief_summary->'expectationsAndPromises'->'leadGenerationExpectations'->>'qualityExpectations' AS quality_expectations,\n  hb.brief_summary->'expectationsAndPromises'->'leadGenerationExpectations'->>'timelineExpectations' AS timeline_expectations,\n  \n  -- Service Delivery Expectations\n  hb.brief_summary->'expectationsAndPromises'->'serviceDeliveryExpectations'->>'frequencyOfService' AS frequency_of_service,\n  hb.brief_summary->'expectationsAndPromises'->'serviceDeliveryExpectations'->>'successMetricsDiscussed' AS success_metrics_discussed,\n  hb.brief_summary->'expectationsAndPromises'->'serviceDeliveryExpectations'->>'whatClientExpectsFromUs' AS what_client_expects,\n  \n  -- Support Communication Expectations\n  hb.brief_summary->'expectationsAndPromises'->'supportCommunicationExpectations'->>'ongoingSupportLevel' AS ongoing_support_level,\n  hb.brief_summary->'expectationsAndPromises'->'supportCommunicationExpectations'->>'responseTimePromised' AS response_time_promised,\n  hb.brief_summary->'expectationsAndPromises'->'supportCommunicationExpectations'->>'trainingOnboardingPromised' AS training_onboarding_promised,\n\n  -- ============ CLIENT STRENGTHS EXPANSION ============\n  hb.brief_summary->'clientStrengthsExpansion'->>'topStrengths' AS top_strengths,\n  hb.brief_summary->'clientStrengthsExpansion'->>'growthOpportunities' AS growth_opportunities,\n\n  -- ============ LEAD EXPECTATION MOTIVATION ============\n  hb.brief_summary->'leadExpectationMotivation'->>'reasonForPayment' AS reason_for_payment,\n  hb.brief_summary->'leadExpectationMotivation'->>'leadPredictorScore' AS lead_predictor_score,\n  hb.brief_summary->'leadExpectationMotivation'->>'expectationSettingHighlights' AS expectation_setting_highlights,\n  hb.brief_summary->'leadExpectationMotivation'->>'expectationVsPredictionAlignment' AS expectation_vs_prediction_alignment,\n\n  -- ============ RED FLAGS AND RISK INDICATORS ============\n  hb.brief_summary->'redFlagsAndRiskIndicators'->>'otherRedFlags' AS other_red_flags,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->>'overallRiskScore' AS overall_risk_score,\n  \n  -- Over Promising Indicators\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overPromisingIndicators'->>'flag' AS overpromising_flag,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overPromisingIndicators'->>'riskLevel' AS overpromising_risk_level,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overPromisingIndicators'->>'specificPromises' AS overpromising_specific,\n  \n  -- Payment Hesitation Signals\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'paymentHesitationSignals'->>'flag' AS payment_hesitation_flag,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'paymentHesitationSignals'->>'details' AS payment_hesitation_details,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'paymentHesitationSignals'->>'riskLevel' AS payment_hesitation_risk,\n  \n  -- Guaranteed Results Language\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'guaranteedResultsLanguage'->>'flag' AS guaranteed_results_flag,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'guaranteedResultsLanguage'->>'details' AS guaranteed_results_details,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'guaranteedResultsLanguage'->>'riskLevel' AS guaranteed_results_risk,\n  \n  -- Misaligned Product Understanding\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'misalignedProductUnderstanding'->>'flag' AS misaligned_understanding_flag,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'misalignedProductUnderstanding'->>'details' AS misaligned_understanding_details,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'misalignedProductUnderstanding'->>'riskLevel' AS misaligned_understanding_risk,\n  \n  -- Service Specific Lead Expectations\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'serviceSpecificLeadExpectations'->>'flag' AS service_specific_flag,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'serviceSpecificLeadExpectations'->>'details' AS service_specific_details,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'serviceSpecificLeadExpectations'->>'riskLevel' AS service_specific_risk,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'serviceSpecificLeadExpectations'->>'serviceMentioned' AS service_mentioned,\n  \n  -- Overnight Unrealistic Growth Expectations\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overnightUnrealisticGrowthExpectations'->>'flag' AS unrealistic_growth_flag,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overnightUnrealisticGrowthExpectations'->>'details' AS unrealistic_growth_details,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overnightUnrealisticGrowthExpectations'->>'riskLevel' AS unrealistic_growth_risk,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overnightUnrealisticGrowthExpectations'->>'expectedTimeline' AS unrealistic_expected_timeline,\n  hb.brief_summary->'redFlagsAndRiskIndicators'->'overnightUnrealisticGrowthExpectations'->>'currentVsExpectedRevenue' AS current_vs_expected_revenue,\n\n  -- ============ REVIEW REPUTATION READINESS ============\n  hb.brief_summary->'reviewReputationReadiness'->>'reviewCount' AS review_count,\n  hb.brief_summary->'reviewReputationReadiness'->>'googleRating' AS google_rating,\n  hb.brief_summary->'reviewReputationReadiness'->>'issuesWithReviews' AS issues_with_reviews,\n  hb.brief_summary->'reviewReputationReadiness'->>'reviewAutomationExplained' AS review_automation_explained,\n  hb.brief_summary->'reviewReputationReadiness'->>'reviewStrategyAcknowledged' AS review_strategy_acknowledged,\n\n  -- ============ DELIVERABLES AND COMMITMENTS ============\n  hb.brief_summary->'deliverablesAndCommitments'->>'criticalDependencies' AS critical_dependencies,\n  hb.brief_summary->'deliverablesAndCommitments'->>'successCriteriaExpected' AS success_criteria_expected,\n  hb.brief_summary->'deliverablesAndCommitments'->>'ongoingDeliverablesExpected' AS ongoing_deliverables_expected,\n  hb.brief_summary->'deliverablesAndCommitments'->>'immediateDeliverablesExpected' AS immediate_deliverables_expected,\n  hb.brief_summary->'deliverablesAndCommitments'->>'shortTermDeliverablesExpected' AS short_term_deliverables_expected,\n\n  -- ============ GREY AREAS AND UNRESOLVED QUESTIONS ============\n  hb.brief_summary->'greyAreasAndUnresolvedQuestions'->>'assumptionsMade' AS assumptions_made,\n  hb.brief_summary->'greyAreasAndUnresolvedQuestions'->>'timelineAmbiguities' AS timeline_ambiguities,\n  hb.brief_summary->'greyAreasAndUnresolvedQuestions'->>'ambiguousCommitments' AS ambiguous_commitments,\n  hb.brief_summary->'greyAreasAndUnresolvedQuestions'->>'unaddressedQuestions' AS unaddressed_questions,\n  hb.brief_summary->'greyAreasAndUnresolvedQuestions'->>'pricingServiceClarityIssues' AS pricing_service_clarity_issues\n\nFROM cx.handover_brief AS hb \nLEFT JOIN cx.am_mapping cam ON hb.entity_id = cam.entity_id\nLEFT JOIN entities.employees eem ON cam.am_entity_id = eem.entity_id\nWHERE hb.entity_id = '__ENTITY_ID__'"},
  {"name":"query26","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"SELECT\n  onboarding_state,\n  created_at::date,\n  is_booking_link_added,\n  is_lead_prediction_viewed\nFROM\n  app.onboarding\nWHERE\n  entity_id = '__ENTITY_ID__'"},
  {"name":"query34","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"SELECT completed_at AS onboarded_date FROM l2b.win_onboarding_status WHERE entity_id = '__ENTITY_ID__' AND status = 'completed' ORDER BY completed_at DESC LIMIT 1"},
  {"name":"query6","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"WITH base AS (\n  SELECT \n    CASE WHEN gl.name IN (select location_name FROM gbp.test_locations) THEN true ELSE ee.is_test END AS is_test,\n    ee.is_active,\n    COALESCE((el.lead_masking->>'status')::boolean, false) AS lead_masking\n  FROM gbp.locations gl \n  JOIN entities.entities ee ON ee.entity_id = gl.entity_id\n  JOIN entities.locations el ON el.entity_id = gl.entity_id\n  WHERE gl.entity_id = '__ENTITY_ID__'\n),\ndead AS (\n  SELECT min(updated_at_timestamp::date) as dead_date FROM entities.entities_logs WHERE entity_id = '__ENTITY_ID__' AND is_active = false\n),\nl2bstatus AS (\n  SELECT status FROM l2b.entities WHERE entity_id = '__ENTITY_ID__'\n)\nSELECT \n  is_test,\n  is_active,\n  lead_masking,\n  dead_date,\n  status as l2bStatus\nFROM base LEFT JOIN dead ON 1=1 LEFT JOIN l2bstatus ON 1=1"},
  {"name":"query7","section":"Onboarding & Team","runnable":true,"deps":[],"sql":"WITH users AS (\n  SELECT er.entity_1_id AS user_entity_id\n  FROM entities.entity_relationships er\n  WHERE er.entity_2_id::text = '__ENTITY_ID__'\n  -- UNION ALL \n  -- SELECT er.entity_2_id AS user_entity_id\n  -- FROM entities.entity_relationships er\n  -- WHERE er.entity_1_id::text = \n)\n\nSELECT \n  entity_id AS user_entity_id,\n  first_name,\n  last_name\nFROM entities.users eu\nJOIN users ON users.user_entity_id = eu.entity_id"},
  {"name":"email_address","section":"Contact, IDs & Requests","runnable":true,"deps":[],"sql":"SELECT email_address FROM entities.emails WHERE entity_id = '__ENTITY_ID__'"},
  {"name":"phone_number","section":"Contact, IDs & Requests","runnable":true,"deps":[],"sql":"SELECT country_code, phone_number::text FROM entities.phones WHERE entity_id = '__ENTITY_ID__'\nAND type <> 'VIRTUAL_NUMBER'"},
  {"name":"query10","section":"Contact, IDs & Requests","runnable":false,"deps":["Location_data","chrone_uid","trial_data","website_id"],"sql":"SELECT 'location entity_id' AS label, {{ select1.value }} AS value\nUNION ALL\nSELECT 'place_id', {{ Location_data.data.place_id[0] }}\nUNION ALL\nSELECT 'location_name', {{ Location_data.data.location_name[0] }}\nUNION ALL\nSELECT 'GBP Title', {{ Location_data.data.gbp_title[0] }}\nUNION ALL\nSELECT 'City', {{ Location_data.data.city[0] }}\nUNION ALL\nSELECT 'State', {{ Location_data.data.state[0] }}\nUNION ALL\nSELECT 'Map Link', {{ Location_data.data.map_link[0] }}\nUNION ALL\nSELECT 'Primary Category Name', {{ Location_data.data.primary_category_name[0] }}\nUNION ALL\nSELECT 'Primary Category Display Name', {{ Location_data.data.primary_category_display_name[0] }}\nUNION ALL\nSELECT 'Website URI', {{ Location_data.data.website_uri[0] }}\nUNION ALL\nSELECT 'GBP Description', {{ Location_data.data.gbp_description[0] }}\nUNION ALL\nSELECT 'Opening Date', {{ Location_data.data.opening_date[0] }}\nUNION ALL\nSELECT 'Custom Completion Score', {{ Location_data.data.custom_completion_score[0] }}\nUNION ALL\nSELECT 'Created At', {{ Location_data.data.created_at[0] }}\nUNION ALL\nSELECT 'Chrone Created At', {{ chrone_uid.data.created_at[0] }}\nUNION ALL\nSELECT 'verification_status', {{ Location_data.data.verification_status[0] }}\nUNION ALL\nSELECT 'review_link', {{ Location_data.data.review_link[0] }}\nUNION ALL\nSELECT 'website_id', {{ website_id.data.website_id[0] }}\nUNION ALL \nSELECT 'trial_start_date', {{ trial_data.data.trial_start_date[0] }}\nUNION ALL\nSELECT 'duplicate_location', {{ Location_data.data.duplicate_location[0] }}\nUNION ALL\nSELECT 'Appointment URL', {{ Location_data.data.appointment_url[0] }}\nUNION ALL\nSELECT 'Menu URL', {{ Location_data.data.menu_url[0] }}"},
  {"name":"requests","section":"Contact, IDs & Requests","runnable":true,"deps":[],"sql":"SELECT \n  status, priority, details, request_type, created_at::date, additional_data\nFROM requests.requests\nWHERE entity_id = '__ENTITY_ID__'\nAND is_active = true"},
  {"name":"website","section":"Contact, IDs & Requests","runnable":false,"deps":["website_id"],"sql":"WITH base AS (\nSELECT website_id, custom_domain FROM website.custom_domains WHERE is_active = true\n)\n\nSELECT ww.website_id, COALESCE(custom_domain,url) as url \nFROM website.websites ww \nLEFT JOIN base ON ww.website_id = base.website_id\nWHERE ww.website_id = {{ website_id.data.website_id[0] }}"},
  {"name":"website_id","section":"Contact, IDs & Requests","runnable":true,"deps":[],"sql":"SELECT location_id, website_id\nFROM website.locations           \nWHERE location_id = '__ENTITY_ID__'"},
];
